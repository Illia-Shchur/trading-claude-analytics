import assert from 'node:assert/strict'
import { generateKeyPairSync } from 'node:crypto'
import { appendExposureLedger, appendProspectiveEvent, assignPitTier, freezeNextPrecommit, generateNextCandidates, makeActivationArtifact, makeExecutionPolicy, makePortfolioPolicy, makeProspectiveLedger, makeProspectiveReservation, makeStackContract, monitorProspective, readinessAudit, simulateResearchPortfolio, stationaryBlockMaxStatistic, validateExecutionPolicy, validatePortfolioPolicy, verifyActivationArtifact, validateProspectiveLedger, nestedWalkForward, withHash } from '../tools/strategy-research-next.mjs'

const precommit = freezeNextPrecommit({
  schema: 'strategy-precommit/1', precommit_id: 'next-fixture', phenomenon: 'forced deleveraging creates temporary inventory imbalance', mechanism: 'constrained sellers transfer inventory to patient liquidity', forced_actor: 'leveraged seller', edge_consumer: 'patient liquidity', direction: 'long', horizon: '3-30 days', expected_signal_frequency: { min: 2, max: 20, unit: 'per year' }, expected_win_rate: { min: 0.35, max: 0.65 }, expected_payoff: { average_win_r: 1.5, average_loss_r: 1 }, work_regimes: ['high liquidation'], fail_regimes: ['thin unavailable data'], required_inputs: [{ id: 'bars', evidence_family: 'price', availability: 'completed bar' }], simplest_falsifier: { named_null: 'no rebound after forced selling', threshold: 0 }, independence_replication_groups: ['asset', 'episode'], composite_score_deferred: true, tradable_instrument_contract: { instruments: [{ asset: 'btc', instrument_type: 'spot' }] }
})
assert.equal(assignPitTier({ source: 'custom', requestedPitTier: 'IMMUTABLE_EVENT_ARCHIVE' }).assigned_pit_tier, 'UNVERIFIED_DEVELOPMENT_ONLY')
assert.equal(assignPitTier({ source: 'binance:spot-ohlcv', requestedPitTier: 'UNVERIFIED_DEVELOPMENT_ONLY' }).assigned_pit_tier, 'IMMUTABLE_EVENT_ARCHIVE')
const candidates = generateNextCandidates({ precommit, method: 'GRID', grid: { stop: [1, 2], target: [1, 2] }, seed: 7 })
assert.equal(candidates.declared_k, 4); assert.equal(candidates.effective_k, 4)
const exposure = appendExposureLedger({ hypothesisFamily: 'fixture-family', datasetRootSha256: 'a'.repeat(64), candidates: candidates.candidates })
const rerun = appendExposureLedger({ prior: exposure, hypothesisFamily: 'fixture-family', datasetRootSha256: 'a'.repeat(64), candidates: [{ candidate_id: 'renamed', behavior_sha256: candidates.candidates[0].behavior_sha256 }] })
assert.equal(rerun.cumulative_k, 4); assert.throws(() => appendExposureLedger({ prior: exposure, hypothesisFamily: 'fixture-family', datasetRootSha256: 'b'.repeat(64), candidates: [] }), /dataset root/)
const execution = makeExecutionPolicy(); validateExecutionPolicy(execution); const portfolio = makePortfolioPolicy(); validatePortfolioPolicy(portfolio)
const stat = stationaryBlockMaxStatistic({ a: { e1: 1, e2: 0, e3: 1 }, b: { e1: 0, e2: 0, e3: 0 } }, { iterations: 20, seed: 2 }); assert.equal(stat.synchronized_draws, true); assert.equal(stat.spa, false)
const wfo = nestedWalkForward({ candidates: candidates.candidates.slice(0, 2), folds: [{ fold_id: '1', train_start: 0, train_end: 10, test_start: 20, test_end: 30 }], purgeMs: 5, embargoMs: 5, evaluator: (candidate, context) => ({ expectancy_r: context.phase === 'TRAIN' ? Number(candidate.definition.stop) : 0 }) }); assert.equal(wfo.folds[0].selected_candidate_id, candidates.candidates[0].candidate_id)
const reservation = makeProspectiveReservation({ frozenAt: '2020-01-01T00:00:00Z', startAt: '2020-01-02T00:00:00Z', lineage: { strategy: 'a'.repeat(64) }, proposedAssets: ['btc'], monitoringContract: { monitoring_pass: true, statistical_pass: true, stress_pass: true, portfolio_pass: true } }); let ledger = makeProspectiveLedger(reservation); ledger = appendProspectiveEvent(ledger, { kind: 'SIGNAL', decisionTime: '2020-01-03T00:00:00Z', payload: { signal_id: 's1', asset: 'btc', direction: 'long', decision: 'CANDIDATE_REVIEW', horizon_ms: 3_600_000, availability_receipt_sha256: 'b'.repeat(64), capture_time: '2020-01-03T00:00:00Z', lineage_sha256: reservation.lineage_sha256 } }); ledger = appendProspectiveEvent(ledger, { kind: 'OUTCOME', decisionTime: '2020-01-03T00:00:00Z', outcomeTime: '2020-01-04T00:00:00Z', payload: { signal_id: 's1', asset: 'btc', entry_time: '2020-01-03T00:00:00Z', exit_time: '2020-01-04T00:00:00Z', net_pnl: 1, availability_receipt_sha256: 'b'.repeat(64), capture_time: '2020-01-04T00:00:00Z', lineage_sha256: reservation.lineage_sha256 } }); validateProspectiveLedger(ledger); assert.throws(() => appendProspectiveEvent(ledger, { kind: 'SIGNAL', decisionTime: '2020-01-01T00:00:00Z' }), /pre-freeze/)
assert.equal(monitorProspective({ ledger, expected: { expectancy: 0 }, now: '2020-01-05T00:00:00Z' }).actual.completed_trades, 1)
assert.throws(() => makeActivationArtifact({ strategySha256: 'a'.repeat(64), candidateSha256: candidates.content_sha256, riskPolicySha256: portfolio.content_sha256, evidenceArtifacts: {} }), /private key/)
const audit = readinessAudit(); assert.equal(audit.active_strategy_count, 0); assert.equal(audit.dimensions.length, 7)
const result = simulateResearchPortfolio({ trades: [{ trade_id: 't1', asset: 'btc', direction: 'long', entry_time: 1, exit_time: 2, notional: 100, risk_amount: 10, net_pnl: 2, instrument: { instrument_type: 'spot' } }], policy: portfolio, initialEquity: 1000, bootstrapIterations: 10 }); assert.equal(result.activation, 'RESEARCH_ONLY')
console.log('strategy-research-next-test: ok')
