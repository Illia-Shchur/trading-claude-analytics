{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "strategy-evaluator-operating-characteristics-corrected-successor-evaluator-receipt/1",
  "type": "object",
  "required": ["schema", "version", "origin", "result_schema", "result_content_sha256", "economic_semantic_sha256", "status", "executor_identity_sha256", "source_input_sha256", "metrics", "accounting_version", "evaluator_identity", "legacy_result_content_sha256", "content_sha256"],
  "properties": {
    "schema": {"const": "strategy-evaluator-operating-characteristics-corrected-successor-evaluator-receipt/1"},
    "version": {"const": 1},
    "origin": {"const": "SHARED_FIXED_EVALUATOR_IN_PROCESS"},
    "result_schema": {"const": "strategy-fixed-baseline-corrected-result/1"},
    "result_content_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "economic_semantic_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "status": {"type": "string"},
    "executor_identity_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "source_input_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "metrics": {"type": "object"},
    "accounting_version": {"const": "PORTFOLIO_ACCOUNTING_CORRECTION_V1"},
    "evaluator_identity": {"const": "com.tradinganalytics.research.v5.StrategyFixedBaselineCorrectedV1"},
    "legacy_result_content_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "content_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
  },
  "additionalProperties": false
}
