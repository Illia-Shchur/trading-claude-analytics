{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "strategy-fixed-baseline-non-trading/1",
  "type": "object",
  "required": ["schema", "version", "status", "policy_id", "provenance", "venue", "instrument", "intervals"],
  "properties": {
    "schema": {"const": "strategy-fixed-baseline-non-trading/1"},
    "version": {"const": 1},
    "status": {"const": "FROZEN"},
    "venue": {"const": "BINANCE"},
    "instrument": {"const": "BINANCE_SPOT"},
    "intervals": {"type": "array"},
    "content_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
  },
  "additionalProperties": true
}
