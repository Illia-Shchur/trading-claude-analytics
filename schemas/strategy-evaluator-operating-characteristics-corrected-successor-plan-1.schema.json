{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "strategy-evaluator-operating-characteristics-corrected-successor-plan/1",
  "type": "object",
  "required": ["schema", "version", "evidence_phase", "decision", "development_exposure", "promotion_eligible", "activation_authorized", "outcomes_opened", "binding_fixed_evaluator", "corrected_evaluator_identity", "accounting_version", "baseline_sha256", "control_spec_sha256", "experiment_sha256", "executor_source_sha256", "executor_identity_sha256", "cells", "cell_count", "replications", "episodes_per_replication", "cluster_count", "paired_cluster_count", "lifecycle_series_per_replication", "horizon_minutes", "resource_budget", "resource_preflight", "frozen_before_outcomes", "content_sha256"],
  "properties": {
    "schema": {"const": "strategy-evaluator-operating-characteristics-corrected-successor-plan/1"},
    "version": {"const": 1},
    "evidence_phase": {"const": "DEVELOPMENT"},
    "decision": {"const": "DIAGNOSTIC_ONLY"},
    "development_exposure": {"const": true},
    "promotion_eligible": {"const": false},
    "activation_authorized": {"const": false},
    "outcomes_opened": {"const": false},
    "frozen_before_outcomes": {"const": true},
    "binding_fixed_evaluator": {"const": "com.tradinganalytics.research.v5.StrategyFixedBaselineCorrectedV1"},
    "corrected_evaluator_identity": {"const": "com.tradinganalytics.research.v5.StrategyFixedBaselineCorrectedV1"},
    "accounting_version": {"const": "PORTFOLIO_ACCOUNTING_CORRECTION_V1"},
    "baseline_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "control_spec_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "experiment_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "executor_source_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "executor_identity_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "cells": {"type": "array", "minItems": 4, "maxItems": 4, "items": {"type": "object"}},
    "cell_count": {"const": 4},
    "replications": {"const": 75},
    "episodes_per_replication": {"const": 450},
    "cluster_count": {"const": 288},
    "paired_cluster_count": {"const": 162},
    "lifecycle_series_per_replication": {"type": "integer", "minimum": 1},
    "horizon_minutes": {"type": "integer", "minimum": 1},
    "resource_budget": {"type": "object"},
    "resource_preflight": {"type": "object"},
    "content_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
  },
  "additionalProperties": true
}
