#!/usr/bin/env node
/* Capture real GitHub API responses for the v5 deployment audit.  The exit
 * status of `gh` is not an HTTP status; --include is parsed explicitly. */
import { execFileSync } from 'node:child_process'
import { writeFileSync } from 'node:fs'
import { createHash, createPrivateKey, createPublicKey, createSign, verify as verifySignature } from 'node:crypto'
import { makeDeploymentSettingsCaptureV5 } from './strategy-research-v5.mjs'
import { captureFailureReason, firstNon200Endpoint, selectCaptureStatus } from './github-capture-policy.mjs'
import { WRITER_APP_ID } from './verify-evidence-writer-installation.mjs'
import canonicalize from 'canonicalize'

const digest = value => createHash('sha256').update(typeof value === 'string' ? value : canonicalize(value)).digest('hex')

function api(path, token = process.env.GH_TOKEN) {
  let output = ''
  try { output = execFileSync('gh', ['api', '--include', path], { encoding: 'utf8', env: { ...process.env, GH_TOKEN: token }, stdio: ['ignore', 'pipe', 'pipe'] }) } catch (error) { output = String(error.stdout || error.stderr || '') }
  const match = output.match(/HTTP\/\d(?:\.\d)?\s+(\d{3})/i); const bodyText = output.split(/\r?\n\r?\n/).at(-1) || '{}'; let body = {}; try { body = JSON.parse(bodyText) } catch {}
  return { status: Number(match?.[1] || 0), body }
}

const repository = process.env.GITHUB_REPOSITORY
if (!repository) throw new Error('GITHUB_REPOSITORY is required')
const settingsTokenKind = String(process.env.V5_SETTINGS_TOKEN_KIND || '').toUpperCase()
const SETTINGS_AUDITOR_APP_ID = 4716635
const SETTINGS_AUDITOR_INSTALLATION_ID = 156531963
const SETTINGS_AUDITOR_APP_SLUG = 'strategy-v5-settings-auditor'
const configuredAuditorAppId = String(process.env.V5_SETTINGS_AUDITOR_APP_ID || '')
const configuredAuditorInstallationId = String(process.env.V5_SETTINGS_AUDITOR_INSTALLATION_ID || '')
const auditorIdentityConfigured = configuredAuditorAppId === String(SETTINGS_AUDITOR_APP_ID) && configuredAuditorInstallationId === String(SETTINGS_AUDITOR_INSTALLATION_ID)
const auditorAppId = SETTINGS_AUDITOR_APP_ID
const auditorInstallationId = SETTINGS_AUDITOR_INSTALLATION_ID
const auditorAppSlug = SETTINGS_AUDITOR_APP_SLUG
const auditorPermissionContract = Object.freeze({ actions: 'read', administration: 'read', environments: 'read', metadata: 'read', secrets: 'read' })
const exactAuditorPermissions = value => value && typeof value === 'object' && !Array.isArray(value) && Object.keys(value).length === Object.keys(auditorPermissionContract).length && Object.entries(auditorPermissionContract).every(([key, expected]) => value[key] === expected)
const exactEvents = value => Array.isArray(value) && value.length === 0
const positiveInteger = value => Number.isSafeInteger(Number(value)) && Number(value) > 0
function auditorAppJwt(appId, privateKeyPem) {
  const now = Math.floor(Date.now() / 1000); const encode = value => Buffer.from(JSON.stringify(value)).toString('base64url'); const header = encode({ alg: 'RS256', typ: 'JWT' }); const payload = encode({ iat: now - 60, exp: now + 540, iss: Number(appId) }); const input = `${header}.${payload}`; const signer = createSign('RSA-SHA256'); signer.update(input); return `${input}.${signer.sign(createPrivateKey(privateKeyPem)).toString('base64url')}`
}
if (process.env.V5_REQUIRE_SETTINGS_TOKEN === 'true' && !process.env.GH_TOKEN) throw new Error('a protected settings token is required; refusing to use an unbound default token')
if (process.env.V5_REQUIRE_SETTINGS_TOKEN === 'true' && !['PAT', 'APP'].includes(settingsTokenKind)) throw new Error('V5_SETTINGS_TOKEN_KIND must be explicitly PAT or APP')
if (process.env.V5_REQUIRE_SETTINGS_AUDITOR === 'true' && (settingsTokenKind !== 'APP' || !process.env.V5_SETTINGS_AUDITOR_APP_ID || !process.env.V5_SETTINGS_AUDITOR_INSTALLATION_ID || !process.env.V5_GITHUB_SETTINGS_AUDITOR_APP_PRIVATE_KEY_PEM)) throw new Error('V5_SETTINGS_AUDITOR_APP_PRIVATE_KEY_PEM and pinned auditor App/installation ids are required; refusing PAT-only settings custody')
if (process.env.V5_REQUIRE_SETTINGS_AUDITOR === 'true' && !auditorIdentityConfigured) throw new Error('auditor App/installation environment must exactly match the frozen deployment identity')
const declaredVisibility = String(process.env.V5_REPOSITORY_VISIBILITY || '').toUpperCase()
if (process.env.V5_REQUIRE_SETTINGS_TOKEN === 'true' && !['PUBLIC', 'PRIVATE'].includes(declaredVisibility)) throw new Error('V5_REPOSITORY_VISIBILITY must be explicitly declared as PUBLIC or PRIVATE')
const evidenceBranch = process.env.V5_EVIDENCE_BRANCH || 'strategy-v5-evidence'
const repositoryApi = api(`repos/${repository}`)
const branch = api(`repos/${repository}/branches/${encodeURIComponent(evidenceBranch)}/protection`)
const branchHead = api(`repos/${repository}/branches/${encodeURIComponent(evidenceBranch)}`)
const environment = api(`repos/${repository}/environments/prospective-v5`)
const writerEnvironment = api(`repos/${repository}/environments/evidence-writer-v5`)
const rulesets = api(`repos/${repository}/rulesets?includes_parents=true`)
// A PAT cannot authenticate the App-installation endpoint.  Keep this
// explicitly unproven; the protected writer job supplies the separate
// freshly-minted-App installation receipt.
const installation = settingsTokenKind === 'PAT' ? { status: 0, body: { skipped_for: 'PAT', installation_proof: 'UNPROVEN' } } : api(`repos/${repository}/installation`)
const settingsTokenIdentity = settingsTokenKind === 'PAT' ? api('user') : api('installation')
const rawRulesetRows = Array.isArray(rulesets.body) ? rulesets.body : (Array.isArray(rulesets.body?.rulesets) ? rulesets.body.rulesets : [])
const rulesetDetails = rawRulesetRows.map(row => ({ id: Number(row.id), ...api(`repos/${repository}/rulesets/${encodeURIComponent(String(row.id))}`) }))
const oidc = api(`repos/${repository}/actions/oidc/customization/sub`)
const actionsPermissions = api(`repos/${repository}/actions/permissions`)
const actionsSelectedPermissions = api(`repos/${repository}/actions/permissions/selected-actions`)
const actionsWorkflowPermissions = api(`repos/${repository}/actions/permissions/workflow`)
const actionsSecretName = process.env.V5_ACTIONS_ATTESTATION_SECRET_NAME || 'PROD_V5_ACTIONS_ATTESTATION_PRIVATE_KEY_B64'
const actionsSecret = api(`repos/${repository}/environments/prospective-v5/secrets/${encodeURIComponent(actionsSecretName)}`)
const repositorySecret = api(`repos/${repository}/actions/secrets/${encodeURIComponent(actionsSecretName)}`)
const organization = repository.split('/')[0]
const organizationSecret = api(`orgs/${encodeURIComponent(organization)}/actions/secrets/${encodeURIComponent(actionsSecretName)}`)
const defaultSettingsTokenSecretName = settingsTokenKind === 'APP' ? 'V5_GITHUB_SETTINGS_AUDITOR_APP_PRIVATE_KEY_PEM' : 'V5_GITHUB_SETTINGS_PAT'
if (settingsTokenKind === 'APP' && process.env.V5_SETTINGS_TOKEN_SECRET_NAME && process.env.V5_SETTINGS_TOKEN_SECRET_NAME !== defaultSettingsTokenSecretName) throw new Error(`APP settings custody must use the protected auditor secret ${defaultSettingsTokenSecretName}`)
const settingsTokenSecretName = settingsTokenKind === 'APP' ? defaultSettingsTokenSecretName : (process.env.V5_SETTINGS_TOKEN_SECRET_NAME || defaultSettingsTokenSecretName)
const settingsTokenSecret = api(`repos/${repository}/environments/prospective-v5/secrets/${encodeURIComponent(settingsTokenSecretName)}`)
const settingsTokenRepositorySecret = api(`repos/${repository}/actions/secrets/${encodeURIComponent(settingsTokenSecretName)}`)
const settingsTokenOrganizationSecret = api(`orgs/${encodeURIComponent(organization)}/actions/secrets/${encodeURIComponent(settingsTokenSecretName)}`)
const evidenceWriterSecretName = process.env.V5_EVIDENCE_WRITER_APP_PRIVATE_KEY_SECRET_NAME || 'V5_EVIDENCE_WRITER_APP_PRIVATE_KEY_PEM'
const evidenceWriterSecret = api(`repos/${repository}/environments/evidence-writer-v5/secrets/${encodeURIComponent(evidenceWriterSecretName)}`)
const evidenceWriterRepositorySecret = api(`repos/${repository}/actions/secrets/${encodeURIComponent(evidenceWriterSecretName)}`)
const evidenceWriterOrganizationSecret = api(`orgs/${encodeURIComponent(organization)}/actions/secrets/${encodeURIComponent(evidenceWriterSecretName)}`)
const auditorAccountId = repositoryApi.body?.owner?.id ?? repositoryApi.body?.owner_id ?? null
const auditorAccountLogin = repository.split('/')[0]
const auditorAccountType = repositoryApi.body?.owner?.type || null
const auditorProofBase = { token_kind: settingsTokenKind, expected_app_id: auditorIdentityConfigured ? SETTINGS_AUDITOR_APP_ID : null, expected_installation_id: auditorIdentityConfigured ? SETTINGS_AUDITOR_INSTALLATION_ID : null, expected_app_slug: auditorAppSlug, app_endpoint_status: 0, app_endpoint_body_sha256: digest({}), installation_endpoint_status: 0, installation_endpoint_body_sha256: digest({}), repositories_endpoint_status: 0, repositories_endpoint_body_sha256: digest({}), app_id: null, app_slug: null, installation_id: null, repository_selection: null, account: null, permissions: null, installation_permissions: null, events: null, installation_events: null, accessible_repository_count: 0, accessible_repository: null, verified: false }
let settingsAuditorInstallation = auditorProofBase
if (settingsTokenKind === 'APP' && auditorIdentityConfigured && process.env.V5_GITHUB_SETTINGS_AUDITOR_APP_PRIVATE_KEY_PEM) {
  const auditorJwt = auditorAppJwt(auditorAppId, process.env.V5_GITHUB_SETTINGS_AUDITOR_APP_PRIVATE_KEY_PEM)
  const appMetadata = api('app', auditorJwt)
  const installationMetadata = api(`app/installations/${auditorInstallationId}`, auditorJwt)
  const accessibleRepositories = api('installation/repositories')
  const appBody = appMetadata.body || {}; const installationBody = installationMetadata.body || {}; const repositoriesBody = accessibleRepositories.body || {}; const rows = Array.isArray(repositoriesBody.repositories) ? repositoriesBody.repositories : []; const row = rows[0] || {}; const account = installationBody.account || {}
  const exactAccount = Number(account.id) === Number(auditorAccountId) && String(account.login || '') === auditorAccountLogin && (!auditorAccountType || String(account.type || '') === String(auditorAccountType))
  const exactRepo = Number(row.id) === Number(repositoryApi.body?.id) && String(row.full_name || '') === repository
  settingsAuditorInstallation = { ...auditorProofBase, app_endpoint_status: appMetadata.status, app_endpoint_body_sha256: digest(appMetadata.body), installation_endpoint_status: installationMetadata.status, installation_endpoint_body_sha256: digest(installationMetadata.body), repositories_endpoint_status: accessibleRepositories.status, repositories_endpoint_body_sha256: digest(accessibleRepositories.body), app_id: Number(appBody.id) || null, app_slug: typeof appBody.slug === 'string' ? appBody.slug : null, installation_id: Number(installationBody.id) || null, repository_selection: installationBody.repository_selection || null, account: account.id ? { id: Number(account.id), login: String(account.login || ''), type: String(account.type || '') } : null, permissions: appBody.permissions || null, installation_permissions: installationBody.permissions || null, events: Array.isArray(appBody.events) ? appBody.events : null, installation_events: Array.isArray(installationBody.events) ? installationBody.events : null, accessible_repository_count: Number(repositoriesBody.total_count) || rows.length, accessible_repository: exactRepo ? { id: Number(row.id), full_name: String(row.full_name) } : (row.id || row.full_name ? { id: Number(row.id) || null, full_name: String(row.full_name || '') } : null), verified: appMetadata.status === 200 && installationMetadata.status === 200 && accessibleRepositories.status === 200 && Number(appBody.id) === auditorAppId && String(appBody.slug || '') === auditorAppSlug && exactAuditorPermissions(appBody.permissions) && exactEvents(appBody.events) && Number(installationBody.id) === auditorInstallationId && Number(installationBody.app_id) === auditorAppId && String(installationBody.app_slug || '') === auditorAppSlug && installationBody.repository_selection === 'selected' && exactAuditorPermissions(installationBody.permissions) && exactEvents(installationBody.events) && exactAccount && Number(repositoriesBody.total_count) === 1 && rows.length === 1 && exactRepo }
}
function verifyOidcJwt(jwt) {
  try {
    const [encodedHeader, encodedPayload, encodedSignature] = String(jwt).split('.')
    if (!encodedHeader || !encodedPayload || !encodedSignature) return false
    const header = JSON.parse(Buffer.from(encodedHeader, 'base64url').toString('utf8'))
    if (header.alg !== 'RS256' || typeof header.kid !== 'string') return false
    const jwksRaw = execFileSync('curl', ['--fail', '--silent', '--show-error', 'https://token.actions.githubusercontent.com/.well-known/jwks'], { encoding: 'utf8', stdio: ['ignore', 'pipe', 'ignore'] })
    const jwks = JSON.parse(jwksRaw); const jwk = Array.isArray(jwks.keys) ? jwks.keys.find(key => key.kid === header.kid && key.kty === 'RSA') : null
    if (!jwk) return false
    const key = createPublicKey({ key: jwk, format: 'jwk' })
    return verifySignature('RSA-SHA256', Buffer.from(`${encodedHeader}.${encodedPayload}`), key, Buffer.from(encodedSignature, 'base64url'))
  } catch { return false }
}
function requestOidcIdentity() {
  const requestUrl = process.env.ACTIONS_ID_TOKEN_REQUEST_URL; const requestToken = process.env.ACTIONS_ID_TOKEN_REQUEST_TOKEN
  if (!requestUrl || !requestToken) return null
  try {
    const separator = requestUrl.includes('?') ? '&' : '?'; const raw = execFileSync('curl', ['--fail', '--silent', '--show-error', '-H', `Authorization: bearer ${requestToken}`, `${requestUrl}${separator}audience=strategy-v5`], { encoding: 'utf8', stdio: ['ignore', 'pipe', 'ignore'] }); const value = JSON.parse(raw).value
    if (typeof value !== 'string') return null
    const parts = value.split('.'); if (parts.length !== 3) return null
    const claims = JSON.parse(Buffer.from(parts[1], 'base64url').toString('utf8'))
    return { claims, signatureVerified: verifyOidcJwt(value) }
  } catch { return null }
}
const oidcIdentity = requestOidcIdentity(); const oidcClaims = oidcIdentity?.claims || null; const oidcSubject = oidcClaims?.sub || null; const captureNowSec = Math.floor(Date.now() / 1000)
const expectedRepositoryId = process.env.GITHUB_REPOSITORY_ID ? String(process.env.GITHUB_REPOSITORY_ID) : (repositoryApi.body?.id === undefined ? null : String(repositoryApi.body.id)); const expectedOwnerId = process.env.V5_REPOSITORY_OWNER_ID ? String(process.env.V5_REPOSITORY_OWNER_ID) : (repositoryApi.body?.owner?.id === undefined ? null : String(repositoryApi.body.owner.id)); const expectedImmutableSubject = expectedOwnerId && expectedRepositoryId ? `repo:${repository.split('/')[0]}@${expectedOwnerId}/${repository.split('/')[1]}@${expectedRepositoryId}:environment:prospective-v5` : null; const expectedWorkflowRef = process.env.GITHUB_WORKFLOW_REF || null; const expectedWorkflowSha = process.env.GITHUB_SHA || null; const expectedRunId = process.env.GITHUB_RUN_ID ? String(process.env.GITHUB_RUN_ID) : null; const expectedRunAttempt = process.env.GITHUB_RUN_ATTEMPT ? Number(process.env.GITHUB_RUN_ATTEMPT) : null
const oidcIdentityVerified = Boolean(oidcIdentity?.signatureVerified === true && oidcClaims && expectedImmutableSubject && oidcSubject === expectedImmutableSubject && expectedRepositoryId && String(oidcClaims.repository_id) === expectedRepositoryId && expectedOwnerId && String(oidcClaims.repository_owner_id) === expectedOwnerId && oidcClaims.environment === 'prospective-v5' && oidcClaims.workflow_ref && oidcClaims.workflow_sha && oidcClaims.iss === 'https://token.actions.githubusercontent.com' && (Array.isArray(oidcClaims.aud) ? oidcClaims.aud.includes('strategy-v5') : oidcClaims.aud === 'strategy-v5') && Number.isInteger(oidcClaims.iat) && Number.isInteger(oidcClaims.exp) && oidcClaims.exp > oidcClaims.iat && oidcClaims.iat <= captureNowSec + 60 && oidcClaims.exp >= captureNowSec - 60 && oidcClaims.exp - oidcClaims.iat <= 15 * 60 && (!expectedWorkflowRef || oidcClaims.workflow_ref === expectedWorkflowRef) && (!expectedWorkflowSha || oidcClaims.workflow_sha === expectedWorkflowSha) && (!expectedRunId || String(oidcClaims.run_id) === expectedRunId) && (!expectedRunAttempt || Number(oidcClaims.run_attempt) === expectedRunAttempt))
const oidcClaimPolicyVerified = Array.isArray(oidc.body?.include_claim_keys) && oidc.body.include_claim_keys.length === 2 && new Set(oidc.body.include_claim_keys.map(String)).size === 2 && oidc.body.include_claim_keys.map(String).sort().join(',') === 'context,repo'
// GitHub's built-in Actions integration is not a valid ruleset bypass actor
// for every repository plan.  Production must pin the ID of the dedicated
// installed evidence-writer App explicitly; never silently substitute 15368.
const configuredEvidenceWriterAppId = Number(process.env.V5_EVIDENCE_WRITER_APP_ID || '')
const evidenceWriterAppIdValid = configuredEvidenceWriterAppId === WRITER_APP_ID
const branchBody = branch.body || {}; const rawApps = Array.isArray(branchBody.restrictions?.apps) ? branchBody.restrictions.apps : []; const installed = installation.body?.app || {}; const installedApp = { status: installation.status, id: Number.isInteger(installation.body?.app_id) ? installation.body.app_id : (Number.isInteger(installed.id) ? installed.id : null), slug: typeof installed.slug === 'string' ? installed.slug : null, verified: installation.status === 200 && Number.isInteger(installation.body?.app_id || installed.id) && typeof installed.slug === 'string' }
const expectedSettingsAppId = process.env.V5_GITHUB_SETTINGS_APP_ID ? Number(process.env.V5_GITHUB_SETTINGS_APP_ID) : null; const expectedSettingsUserId = process.env.V5_SETTINGS_TOKEN_USER_ID ? String(process.env.V5_SETTINGS_TOKEN_USER_ID) : null; const expectedSettingsLogin = process.env.V5_SETTINGS_TOKEN_LOGIN ? String(process.env.V5_SETTINGS_TOKEN_LOGIN) : null
const settingsTokenIdentityPinned = settingsTokenKind === 'PAT' ? /^\d+$/.test(String(expectedSettingsUserId || '')) && Boolean(expectedSettingsLogin) : Number.isInteger(expectedSettingsAppId)
const settingsTokenIdentityVerified = settingsTokenKind === 'PAT'
  ? settingsTokenIdentityPinned && settingsTokenIdentity.status === 200 && Number.isInteger(Number(settingsTokenIdentity.body?.id)) && String(settingsTokenIdentity.body.id) === expectedSettingsUserId && typeof settingsTokenIdentity.body?.login === 'string' && settingsTokenIdentity.body.login === expectedSettingsLogin
  : settingsAuditorInstallation.verified === true && settingsTokenIdentity.status === 200 && Number.isInteger(Number(settingsTokenIdentity.body?.app_id)) && Number(settingsTokenIdentity.body.app_id) === auditorAppId && Number(settingsAuditorInstallation.app_id) === auditorAppId && (process.env.V5_REQUIRE_SETTINGS_TOKEN !== 'true' || Number.isInteger(expectedSettingsAppId) && Number(settingsTokenIdentity.body.app_id) === expectedSettingsAppId)
const repositoryVisibility = declaredVisibility || (repositoryApi.body?.private === true ? 'PRIVATE' : 'PUBLIC')
const repositoryVisibilityVerified = repositoryApi.status === 200 && ['PUBLIC', 'PRIVATE'].includes(repositoryVisibility) && ((repositoryVisibility === 'PRIVATE') === (repositoryApi.body?.private === true))
const normalizedRestrictions = { users: (Array.isArray(branchBody.restrictions?.users) ? branchBody.restrictions.users : []).map(row => String(row.login || row)).sort(), teams: (Array.isArray(branchBody.restrictions?.teams) ? branchBody.restrictions.teams : []).map(row => String(row.slug || row)).sort(), apps: rawApps.map(row => String(row.slug || row.name || row.id)).sort(), app_ids: rawApps.map(row => Number(row.id)).filter(Number.isInteger).sort((a, b) => a - b), apps_verified: installedApp.verified && rawApps.length > 0 && rawApps.every(row => Number(row.id) === installedApp.id || row.slug === installedApp.slug), installed_app: installedApp }
const normalizedBranchProtection = { api_status: branch.status, enforce_admins: branchBody.enforce_admins?.enabled === true, required_pull_request_reviews: branchBody.required_pull_request_reviews !== null && branchBody.required_pull_request_reviews !== undefined, required_status_checks: branchBody.required_status_checks !== null && branchBody.required_status_checks !== undefined, allow_force_pushes: branchBody.allow_force_pushes?.enabled === true, allow_deletions: branchBody.allow_deletions?.enabled === true, required_linear_history: branchBody.required_linear_history?.enabled === true, restrictions: normalizedRestrictions }
const refMatches = value => { const text = String(value || ''); return text === evidenceBranch || text === `refs/heads/${evidenceBranch}` }
const detailRows = rulesetDetails.map(row => {
  const rules = Array.isArray(row.body?.rules) ? row.body.rules : []
  const ruleTypes = rules.map(rule => String(rule?.type || '').toLowerCase())
  const pullRequest = rules.find(rule => String(rule?.type || '').toLowerCase() === 'pull_request')
  const statusChecks = rules.find(rule => String(rule?.type || '').toLowerCase() === 'required_status_checks')
  const requiredStatusChecks = Array.isArray(statusChecks?.parameters?.required_status_checks) ? statusChecks.parameters.required_status_checks : []
  const exactCustodyStatusRequired = requiredStatusChecks.some(check => String(check?.context || '') === 'strategy-v5-evidence-custody' && Number(check?.integration_id) === 15368)
  const strictStatusChecks = statusChecks?.parameters?.strict_required_status_checks_policy === true
  const refs = Array.isArray(row.body?.conditions?.ref_name?.include) ? row.body.conditions.ref_name.include.map(String).sort() : []
  const exactEvidenceRefRequired = refs.length === 1 && refMatches(refs[0])
  const bypassActors = Array.isArray(row.body?.bypass_actors) ? row.body.bypass_actors.map(actor => ({ type: String(actor?.actor_type || ''), id: Number(actor?.actor_id), mode: String(actor?.bypass_mode || '').toLowerCase() })) : []
  const sortedTypes = [...new Set(ruleTypes)].sort()
  const immutableRulesVerified = exactEvidenceRefRequired && sortedTypes.length === 2 && sortedTypes[0] === 'deletion' && sortedTypes[1] === 'non_fast_forward' && bypassActors.length === 0
  const exactApprovalZero = Number.isInteger(Number(pullRequest?.parameters?.required_approving_review_count)) && Number(pullRequest.parameters.required_approving_review_count) === 0
  const writerRulesVerified = exactEvidenceRefRequired && sortedTypes.length === 2 && sortedTypes[0] === 'pull_request' && sortedTypes[1] === 'required_status_checks' && exactApprovalZero && exactCustodyStatusRequired && strictStatusChecks
  return { id: row.id, status: row.status, target: row.body?.target || null, enforcement: String(row.body?.enforcement || row.body?.enforcement_state || '').toUpperCase() || null, refs, rule_types: sortedTypes, required_status_contexts: requiredStatusChecks.map(check => String(check?.context || '')).sort(), required_status_check_integrations: requiredStatusChecks.map(check => Number(check?.integration_id)).filter(Number.isInteger).sort((a, b) => a - b), strict_status_checks: strictStatusChecks, pull_request_parameters: pullRequest?.parameters || {}, bypass_app_ids: bypassActors.filter(actor => ['Integration', 'integration'].includes(actor.type)).map(actor => actor.id).filter(Number.isInteger).sort((a, b) => a - b), bypass_actors: bypassActors, layer: immutableRulesVerified ? 'IMMUTABLE_CORE' : writerRulesVerified ? 'WRITER_GATE' : null, rules_verified: immutableRulesVerified || writerRulesVerified, immutable_rules_verified: immutableRulesVerified, writer_gate_rules_verified: writerRulesVerified, body_sha256: digest(row.body) }
})
const protectedDetailRows = detailRows.filter(row => (row.target === 'branch' || row.target === 'BRANCH') && row.enforcement === 'ACTIVE' && row.refs.some(refMatches)); const immutableRows = protectedDetailRows.filter(row => row.layer === 'IMMUTABLE_CORE'); const writerGateRows = protectedDetailRows.filter(row => row.layer === 'WRITER_GATE'); const mainRows = detailRows.filter(row => (row.target === 'branch' || row.target === 'BRANCH') && row.enforcement === 'ACTIVE' && row.refs.length === 1 && row.refs[0] === 'refs/heads/main'); const mainPullRequestParameters = mainRows[0]?.pull_request_parameters || {}; const mainPolicyVerified = mainRows.length === 1 && mainRows[0].rule_types.length === 3 && mainRows[0].rule_types[0] === 'deletion' && mainRows[0].rule_types[1] === 'non_fast_forward' && mainRows[0].rule_types[2] === 'pull_request' && Number(mainPullRequestParameters.required_approving_review_count || 0) >= 1 && mainPullRequestParameters.dismiss_stale_reviews_on_push === true && mainPullRequestParameters.require_last_push_approval === true && mainRows[0].bypass_actors.length === 0; const detailStatusesOk = rulesets.status === 200 && rulesetDetails.every(row => row.status === 200)
const immutablePolicyVerified = protectedDetailRows.length === 2 && immutableRows.length === 1 && immutableRows[0].bypass_actors.length === 0
const actionsOnlyBypassVerified = evidenceWriterAppIdValid && protectedDetailRows.length === 2 && writerGateRows.length === 1 && writerGateRows[0].bypass_actors.length === 0 && immutableRows.length === 1 && immutableRows[0].bypass_actors.length === 0
const layeredPolicyVerified = immutablePolicyVerified && actionsOnlyBypassVerified && protectedDetailRows.every(row => row.rules_verified && row.refs.length === 1 && refMatches(row.refs[0]))
let evidenceWriterCredentialConfigured = false
const normalizedRulesets = { api_status: rulesets.status, status: rulesets.status, ids: rawRulesetRows.map(row => Number(row.id)).filter(Number.isInteger).sort(), protected_branch_ids: [...protectedDetailRows.map(row => row.id), ...mainRows.map(row => row.id)].sort((a, b) => a - b), immutable_ruleset_ids: immutableRows.map(row => row.id).sort((a, b) => a - b), writer_gate_ruleset_ids: writerGateRows.map(row => row.id).sort((a, b) => a - b), layers: [...protectedDetailRows.filter(row => row.layer), ...mainRows.map(row => ({ ...row, layer: 'IMMUTABLE_CORE' }))].map(row => ({ id: row.id, layer: row.layer, status: row.status, target: row.target, enforcement: row.enforcement, refs: row.refs, rule_types: row.rule_types, required_status_contexts: row.required_status_contexts, required_status_check_integrations: row.required_status_check_integrations || [], strict_status_checks: row.strict_status_checks === true, pull_request_parameters: row.pull_request_parameters || {}, bypass_actors: row.bypass_actors, body_sha256: row.body_sha256, rules_verified: row.rules_verified })), evidence_writer_app_id: evidenceWriterAppIdValid ? configuredEvidenceWriterAppId : null, evidence_writer_credential_configured: evidenceWriterCredentialConfigured, actions_bypass_app_ids: [...new Set(protectedDetailRows.flatMap(row => row.bypass_app_ids))].sort((a, b) => a - b), protected_ref_matches: protectedDetailRows.length === 2 && protectedDetailRows.every(row => row.refs.length === 1 && refMatches(row.refs[0])), bypass_verified: actionsOnlyBypassVerified, actions_only_bypass_verified: actionsOnlyBypassVerified, immutable_policy_verified: immutablePolicyVerified, writer_gate_policy_verified: writerGateRows.length === 1 && writerGateRows[0].rules_verified && writerGateRows[0].bypass_actors.length === 0, layered_policy_verified: layeredPolicyVerified, enforcement_verified: protectedDetailRows.length === 2 && protectedDetailRows.every(row => row.enforcement === 'ACTIVE'), rules_verified: layeredPolicyVerified, detail_statuses_ok: detailStatusesOk, verified: false }
const actionsSecretSummary = { name: actionsSecretName, environment_status: actionsSecret.status, environment_body_sha256: digest(actionsSecret.body), repository_status: repositorySecret.status, repository_body_sha256: digest(repositorySecret.body), organization_status: organizationSecret.status, organization_body_sha256: digest(organizationSecret.body), verified: actionsSecret.status === 200 && repositorySecret.status === 404 && organizationSecret.status === 404 }
const settingsTokenSecretSummary = { name: settingsTokenSecretName, environment_status: settingsTokenSecret.status, environment_body_sha256: digest(settingsTokenSecret.body), repository_status: settingsTokenRepositorySecret.status, repository_body_sha256: digest(settingsTokenRepositorySecret.body), organization_status: settingsTokenOrganizationSecret.status, organization_body_sha256: digest(settingsTokenOrganizationSecret.body), verified: settingsTokenSecret.status === 200 && settingsTokenRepositorySecret.status === 404 && settingsTokenOrganizationSecret.status === 404 }
const actionsPermissionsSummary = { api_status: actionsPermissions.status, selected_api_status: actionsSelectedPermissions.status, workflow_api_status: actionsWorkflowPermissions.status, allowed_actions: String(actionsPermissions.body?.allowed_actions || ''), sha_pinning_required: actionsPermissions.body?.sha_pinning_required === true, github_owned_allowed: actionsSelectedPermissions.body?.github_owned_allowed === true, verified_allowed: actionsSelectedPermissions.body?.verified_allowed === true, patterns_allowed: Array.isArray(actionsSelectedPermissions.body?.patterns_allowed) ? actionsSelectedPermissions.body.patterns_allowed.map(String).sort() : [], default_workflow_permissions: String(actionsWorkflowPermissions.body?.default_workflow_permissions || ''), can_approve_pull_request_reviews: actionsWorkflowPermissions.body?.can_approve_pull_request_reviews === true, verified: actionsPermissions.status === 200 && actionsSelectedPermissions.status === 200 && actionsWorkflowPermissions.status === 200 && actionsPermissions.body?.allowed_actions === 'selected' && actionsPermissions.body?.sha_pinning_required === true && actionsSelectedPermissions.body?.github_owned_allowed === true && actionsSelectedPermissions.body?.verified_allowed === false && Array.isArray(actionsSelectedPermissions.body?.patterns_allowed) && actionsSelectedPermissions.body.patterns_allowed.length === 0 && actionsWorkflowPermissions.body?.default_workflow_permissions === 'read' && actionsWorkflowPermissions.body?.can_approve_pull_request_reviews === false }
const exactBranchApp = normalizedBranchProtection.restrictions.apps_verified && normalizedBranchProtection.restrictions.users.length === 0 && normalizedBranchProtection.restrictions.teams.length === 0
const legacyBranchSecure = branch.status === 200 && normalizedBranchProtection.enforce_admins && normalizedBranchProtection.required_pull_request_reviews && normalizedBranchProtection.required_status_checks && !normalizedBranchProtection.allow_force_pushes && !normalizedBranchProtection.allow_deletions && exactBranchApp && normalizedBranchProtection.restrictions.apps.length > 0
const rulesetBranchSecure = branchHead.status === 200 && normalizedRulesets.api_status === 200 && normalizedRulesets.layered_policy_verified && normalizedRulesets.protected_ref_matches && normalizedRulesets.immutable_policy_verified && normalizedRulesets.writer_gate_policy_verified && normalizedRulesets.enforcement_verified && normalizedRulesets.rules_verified
const branchSecure = legacyBranchSecure || rulesetBranchSecure
const environmentBody = environment.body || {}; const environmentRules = Array.isArray(environmentBody.protection_rules) ? environmentBody.protection_rules : []; const isReviewerRule = rule => String(rule?.type || '').toLowerCase() === 'required_reviewers'; const requiredReviewerRules = environmentRules.filter(isReviewerRule); const hasConcreteReviewers = rule => isReviewerRule(rule) && ((Array.isArray(rule.reviewers) && rule.reviewers.length > 0) || (Array.isArray(rule.parameters?.reviewers) && rule.parameters.reviewers.length > 0)); const hasSelfReviewProtection = rule => rule?.prevent_self_review === true || rule?.parameters?.prevent_self_review === true; const environmentReviewerRule = environmentRules.find(rule => hasConcreteReviewers(rule) && hasSelfReviewProtection(rule)); const environmentReviewSafe = requiredReviewerRules.length === 0 || Boolean(environmentReviewerRule); const environmentCanAdminsBypass = environmentBody.can_admins_bypass === true; const environmentSecure = environment.status === 200 && environmentBody.can_admins_bypass === false && environmentReviewSafe && environmentBody.deployment_branch_policy?.protected_branches === true && environmentBody.deployment_branch_policy?.custom_branch_policies !== true
const writerEnvironmentBody = writerEnvironment.body || {}; const writerEnvironmentRules = Array.isArray(writerEnvironmentBody.protection_rules) ? writerEnvironmentBody.protection_rules : []; const writerRequiredReviewerRules = writerEnvironmentRules.filter(isReviewerRule); const writerEnvironmentReviewerRule = writerEnvironmentRules.find(rule => hasConcreteReviewers(rule) && hasSelfReviewProtection(rule)); const writerEnvironmentReviewSafe = writerRequiredReviewerRules.length === 0 || Boolean(writerEnvironmentReviewerRule); const writerEnvironmentCanAdminsBypass = writerEnvironmentBody.can_admins_bypass === true; const writerEnvironmentSecure = writerEnvironment.status === 200 && writerEnvironmentBody.can_admins_bypass === false && writerEnvironmentReviewSafe && writerEnvironmentBody.deployment_branch_policy?.protected_branches === true && writerEnvironmentBody.deployment_branch_policy?.custom_branch_policies !== true
const settingsIdentityBody = settingsTokenIdentity.body || {}; const settingsTokenIdentitySummary = { api_status: settingsTokenIdentity.status, app_id: Number.isInteger(Number(settingsIdentityBody.app_id)) ? Number(settingsIdentityBody.app_id) : null, user_id: Number.isInteger(Number(settingsIdentityBody.id)) ? Number(settingsIdentityBody.id) : null, login: typeof settingsIdentityBody.login === 'string' ? settingsIdentityBody.login : null, token_kind: settingsTokenKind, expected_user_id: expectedSettingsUserId && /^\d+$/.test(expectedSettingsUserId) ? Number(expectedSettingsUserId) : null, expected_login: expectedSettingsLogin, secret_name: settingsTokenSecretSummary.name, secret_environment_status: settingsTokenSecretSummary.environment_status, secret_environment_body_sha256: settingsTokenSecretSummary.environment_body_sha256, secret_repository_status: settingsTokenSecretSummary.repository_status, secret_repository_body_sha256: settingsTokenSecretSummary.repository_body_sha256, secret_organization_status: settingsTokenSecretSummary.organization_status, secret_organization_body_sha256: settingsTokenSecretSummary.organization_body_sha256, body_sha256: digest(settingsIdentityBody), verified: settingsTokenIdentityVerified && settingsTokenSecretSummary.verified }
const evidenceWriterSecretSummary = { name: evidenceWriterSecretName, environment_status: evidenceWriterSecret.status, environment_body_sha256: digest(evidenceWriterSecret.body), repository_status: evidenceWriterRepositorySecret.status, repository_body_sha256: digest(evidenceWriterRepositorySecret.body), organization_status: evidenceWriterOrganizationSecret.status, organization_body_sha256: digest(evidenceWriterOrganizationSecret.body), verified: evidenceWriterSecret.status === 200 && evidenceWriterRepositorySecret.status === 404 && evidenceWriterOrganizationSecret.status === 404 && writerEnvironmentSecure }
evidenceWriterCredentialConfigured = evidenceWriterSecretSummary.verified
normalizedRulesets.evidence_writer_credential_configured = evidenceWriterCredentialConfigured
normalizedRulesets.verified = normalizedRulesets.detail_statuses_ok && normalizedRulesets.actions_only_bypass_verified && normalizedRulesets.rules_verified && mainPolicyVerified && evidenceWriterCredentialConfigured
const environmentSummaryForBody = { api_status: environment.status, reviewer_count: environmentRules.filter(hasConcreteReviewers).length, protection_rule_count: environmentRules.length, can_admins_bypass: environmentBody.can_admins_bypass === true, protected_branches: environmentBody.deployment_branch_policy?.protected_branches === true, custom_branch_policies: environmentBody.deployment_branch_policy?.custom_branch_policies === true, prevent_self_review: Boolean(environmentReviewerRule), verified: environmentSecure }
const writerEnvironmentSummary = { api_status: writerEnvironment.status, reviewer_count: writerEnvironmentRules.filter(hasConcreteReviewers).length, protection_rule_count: writerEnvironmentRules.length, can_admins_bypass: writerEnvironmentBody.can_admins_bypass === true, protected_branches: writerEnvironmentBody.deployment_branch_policy?.protected_branches === true, custom_branch_policies: writerEnvironmentBody.deployment_branch_policy?.custom_branch_policies === true, prevent_self_review: Boolean(writerEnvironmentReviewerRule), verified: writerEnvironmentSecure }
const auditorProofRequired = settingsTokenKind === 'APP'; const auditorProofVerified = !auditorProofRequired || settingsAuditorInstallation.verified === true
const allVerified = repositoryVisibilityVerified && branchSecure && branchHead.status === 200 && (legacyBranchSecure || (normalizedRulesets.verified && rulesetBranchSecure)) && environmentSecure && writerEnvironmentSecure && evidenceWriterSecretSummary.verified && oidc.status === 200 && oidc.body?.use_default === false && oidc.body?.use_immutable_subject === true && oidcClaimPolicyVerified && oidcIdentityVerified && actionsSecretSummary.verified && settingsTokenIdentitySummary.verified && actionsPermissionsSummary.verified && auditorProofVerified
const apiBody = { repository: { id: repositoryApi.body?.id ?? null, owner_id: expectedOwnerId, full_name: repositoryApi.body?.full_name || repository, private: repositoryApi.body?.private === true }, repository_visibility: repositoryVisibility, repository_visibility_verified: repositoryVisibilityVerified, branch_protection: normalizedBranchProtection, branch_head: { api_status: branchHead.status, sha: branchHead.body?.commit?.sha || null }, environment_protection: { api_status: environment.status, can_admins_bypass: environmentCanAdminsBypass, protection_rules: environmentRules.map(rule => ({ type: rule?.type || null, reviewers: Array.isArray(rule?.reviewers) ? rule.reviewers.map(reviewer => typeof reviewer === 'string' ? reviewer : ({ id: reviewer?.id ?? null, login: reviewer?.login ?? null, type: reviewer?.type ?? null })) : [], prevent_self_review: rule?.prevent_self_review === true || rule?.parameters?.prevent_self_review === true })), deployment_branch_policy: { protected_branches: environmentBody.deployment_branch_policy?.protected_branches === true, custom_branch_policies: environmentBody.deployment_branch_policy?.custom_branch_policies === true } }, writer_environment_protection: writerEnvironmentSummary, rulesets: normalizedRulesets, actions_permissions: actionsPermissionsSummary, actions_secret: actionsSecretSummary, evidence_writer_secret: evidenceWriterSecretSummary, settings_token_secret: settingsTokenSecretSummary, settings_auditor_installation: settingsAuditorInstallation, installation: installedApp, settings_token_identity: settingsTokenIdentitySummary, oidc: { api_status: oidc.status, use_default: oidc.body?.use_default, use_immutable_subject: oidc.body?.use_immutable_subject === true, include_claim_keys: Array.isArray(oidc.body?.include_claim_keys) ? [...oidc.body.include_claim_keys].sort() : [], signature_verified: oidcIdentity?.signatureVerified === true, claims: oidcClaims ? { repository_id: oidcClaims.repository_id, repository_owner_id: oidcClaims.repository_owner_id ?? expectedOwnerId, environment: oidcClaims.environment, workflow_ref: oidcClaims.workflow_ref, workflow_sha: oidcClaims.workflow_sha, sub: oidcSubject, aud: oidcClaims.aud, iss: oidcClaims.iss, iat: oidcClaims.iat, exp: oidcClaims.exp, run_id: oidcClaims.run_id ?? null, run_attempt: oidcClaims.run_attempt ?? null } : null }, evidence_branch: evidenceBranch }
const endpointStatuses = { repository: repositoryApi.status, branch_protection: branch.status, branch_head: branchHead.status, environment_protection: environment.status, writer_environment_protection: writerEnvironment.status, rulesets: rulesets.status, ruleset_details: detailStatusesOk ? 200 : (rulesetDetails.find(row => row.status !== 200)?.status || rulesets.status), installation: installation.status, settings_token_identity: settingsTokenIdentity.status, settings_token_secret: settingsTokenSecret.status, evidence_writer_secret: evidenceWriterSecret.status, evidence_writer_repository_secret: evidenceWriterRepositorySecret.status, evidence_writer_organization_secret: evidenceWriterOrganizationSecret.status, oidc_subject_restriction: oidc.status, actions_permissions: actionsPermissions.status, actions_selected_permissions: actionsSelectedPermissions.status, actions_workflow_permissions: actionsWorkflowPermissions.status, settings_auditor_app: settingsAuditorInstallation.app_endpoint_status, settings_auditor_installation: settingsAuditorInstallation.installation_endpoint_status, settings_auditor_repositories: settingsAuditorInstallation.repositories_endpoint_status }
// A PAT cannot prove App installation identity, but that is a separate
// runtime writer receipt rather than a malformed GitHub settings response.
// Keep the raw endpoint status in the receipt while excluding this known
// optional PAT proof from the first-failure summary.
const statusForFailure = { ...endpointStatuses, ...(rulesetBranchSecure && branch.status === 404 ? { branch_protection: 200 } : {}), ...(settingsTokenKind === 'PAT' && installation.status !== 200 ? { installation: 200 } : {}), ...(settingsTokenKind === 'PAT' ? { settings_auditor_app: 200, settings_auditor_installation: 200, settings_auditor_repositories: 200 } : {}) }
const firstFailure = firstNon200Endpoint(statusForFailure); const firstFailureReason = captureFailureReason(firstFailure); const captureStatus = selectCaptureStatus({ allVerified, endpointStatuses: statusForFailure })
const receipt = {
  schema: 'github-settings-api-receipt/1', version: 1, repository, captured_at: new Date().toISOString(), evidence_branch: evidenceBranch,
  actions_permissions: actionsPermissionsSummary,
  writer_environment_protection: writerEnvironmentSummary,
  evidence_writer_secret: evidenceWriterSecretSummary,
  actions_secret: actionsSecretSummary,
  rulesets: normalizedRulesets,
  repository_visibility: repositoryVisibility, repository_visibility_verified: repositoryVisibilityVerified,
  settings_token_identity: settingsTokenIdentitySummary,
  settings_token_secret: settingsTokenSecretSummary,
  settings_auditor_installation: settingsAuditorInstallation,
  installation_proof_verified: settingsTokenKind === 'PAT' ? false : settingsAuditorInstallation.verified === true,
  oidc_signature_verified: oidcIdentity?.signatureVerified === true,
  endpoints: {
    repository: { status: repositoryApi.status, body_sha256: digest(repositoryApi.body) },
    branch_protection: { status: branch.status, body_sha256: digest(branch.body) },
    branch_head: { status: branchHead.status, body_sha256: digest(branchHead.body) },
    environment_protection: { status: environment.status, body_sha256: digest(environment.body) },
    writer_environment_protection: { status: writerEnvironment.status, body_sha256: digest(writerEnvironment.body) },
    rulesets: { status: rulesets.status, body_sha256: digest(rulesets.body) },
    ruleset_details: { status: detailStatusesOk ? 200 : (rulesetDetails.find(row => row.status !== 200)?.status || rulesets.status), body_sha256: digest(detailRows) },
    installation: { status: installation.status, body_sha256: digest(installation.body) },
    settings_token_identity: { status: settingsTokenIdentity.status, body_sha256: digest(settingsTokenIdentity.body) },
    settings_token_secret: { status: settingsTokenSecret.status, body_sha256: digest(settingsTokenSecret.body) },
    evidence_writer_secret: { status: evidenceWriterSecret.status, body_sha256: digest(evidenceWriterSecret.body) },
    evidence_writer_repository_secret: { status: evidenceWriterRepositorySecret.status, body_sha256: digest(evidenceWriterRepositorySecret.body) },
    evidence_writer_organization_secret: { status: evidenceWriterOrganizationSecret.status, body_sha256: digest(evidenceWriterOrganizationSecret.body) },
    oidc_subject_restriction: { status: oidc.status, body_sha256: digest(oidc.body) },
    actions_permissions: { status: actionsPermissions.status, body_sha256: digest(actionsPermissions.body) },
    actions_selected_permissions: { status: actionsSelectedPermissions.status, body_sha256: digest(actionsSelectedPermissions.body) },
    actions_workflow_permissions: { status: actionsWorkflowPermissions.status, body_sha256: digest(actionsWorkflowPermissions.body) },
    settings_auditor_app: { status: settingsAuditorInstallation.app_endpoint_status, body_sha256: settingsAuditorInstallation.app_endpoint_body_sha256 },
    settings_auditor_installation: { status: settingsAuditorInstallation.installation_endpoint_status, body_sha256: settingsAuditorInstallation.installation_endpoint_body_sha256 },
    settings_auditor_repositories: { status: settingsAuditorInstallation.repositories_endpoint_status, body_sha256: settingsAuditorInstallation.repositories_endpoint_body_sha256 }
  },
  verified: allVerified,
  blockers: [firstFailureReason, repositoryVisibilityVerified ? null : 'REPOSITORY_VISIBILITY_UNVERIFIED', branchSecure ? null : 'BRANCH_PROTECTION_POLICY_UNVERIFIED', branchHead.status === 200 ? null : 'EVIDENCE_BRANCH_HEAD_UNVERIFIED', evidenceWriterAppIdValid ? null : 'EVIDENCE_WRITER_APP_ID_UNVERIFIED', evidenceWriterCredentialConfigured ? null : 'EVIDENCE_WRITER_APP_CREDENTIAL_UNCONFIGURED', normalizedRulesets.verified ? null : 'RULESETS_UNAVAILABLE_OR_UNVERIFIED', settingsTokenKind === 'PAT' || (installation.status === 200 && installedApp.verified) ? null : 'GITHUB_APP_IDENTITY_UNVERIFIED', auditorProofRequired && !auditorProofVerified ? 'GITHUB_SETTINGS_AUDITOR_INSTALLATION_UNVERIFIED' : null, settingsTokenIdentitySummary.verified ? null : 'GITHUB_SETTINGS_TOKEN_IDENTITY_UNVERIFIED', settingsTokenSecretSummary.verified ? null : 'GITHUB_SETTINGS_TOKEN_SECRET_UNVERIFIED', environmentSecure ? null : 'ENVIRONMENT_PROTECTION_POLICY_UNVERIFIED', writerEnvironmentSecure ? null : 'EVIDENCE_WRITER_ENVIRONMENT_UNVERIFIED', actionsPermissionsSummary.verified ? null : 'GITHUB_ACTIONS_POLICY_UNVERIFIED', oidc.status === 200 && oidcIdentityVerified && oidc.body?.use_default === false && oidc.body?.use_immutable_subject === true && oidcClaimPolicyVerified ? null : 'OIDC_SUBJECT_OR_CLAIM_POLICY_UNVERIFIED', actionsSecretSummary.verified ? null : 'ACTIONS_ONLY_SECRET_UNVERIFIED'].filter(Boolean)
}
receipt.content_sha256 = digest({ ...receipt })
writeFileSync(process.env.V5_SETTINGS_RECEIPT_OUT || 'github-settings-api-receipt.json', JSON.stringify(receipt, null, 2) + '\n', { flag: 'w' })
let capture
try {
  capture = makeDeploymentSettingsCaptureV5({ githubApiResponse: { status: captureStatus, body: apiBody, failure_endpoint: firstFailure?.endpoint || null }, oidcSubject, oidcClaims, oidcSignatureVerified: oidcIdentity?.signatureVerified === true, evidenceBranchHeadSha256: branchHead.body?.commit?.sha || null, evidenceWriterAppId: configuredEvidenceWriterAppId })
} catch (error) {
  const blocked = { schema: 'github-deployment-settings-capture/1', version: 1, captured_at: new Date().toISOString(), api_response: { status: captureStatus, body_sha256: digest(apiBody), provider: 'github-api' }, repository: repository || 'unknown/unknown', repository_id: repositoryApi.body.id ?? 'unknown', repository_private: repositoryApi.body.private === true, repository_visibility: repositoryVisibility, repository_visibility_verified: repositoryVisibilityVerified, evidence_branch: evidenceBranch, evidence_branch_head_sha256: branchHead.body?.commit?.sha ? digest(String(branchHead.body.commit.sha)) : null, oidc_subject: null, oidc_claims: null, oidc_signature_verified: false, branch_protection: { enforce_admins: false, required_pull_request_reviews: false, required_status_checks: false, allow_force_pushes: false, allow_deletions: false, required_linear_history: false, restrictions: { users: [], teams: [], apps: [], app_ids: [], apps_verified: false, installed_app: { status: installation.status, id: null, slug: null, verified: false } }, verified: false }, rulesets: normalizedRulesets, environment_protection: { reviewer_count: 0, protection_rule_count: 0, can_admins_bypass: false, protected_branches: false, custom_branch_policies: false, prevent_self_review: false, verified: false }, writer_environment_protection: { reviewer_count: 0, protection_rule_count: 0, can_admins_bypass: false, protected_branches: false, custom_branch_policies: false, prevent_self_review: false, verified: false }, actions_permissions: actionsPermissionsSummary, actions_secret: actionsSecretSummary, evidence_writer_secret: evidenceWriterSecretSummary, settings_token_secret: settingsTokenSecretSummary, settings_token_identity: { api_status: settingsTokenIdentity.status, app_id: null, user_id: null, login: null, token_kind: settingsTokenKind, expected_user_id: expectedSettingsUserId && /^\d+$/.test(expectedSettingsUserId) ? Number(expectedSettingsUserId) : null, expected_login: expectedSettingsLogin, secret_name: settingsTokenSecretName, secret_environment_status: settingsTokenSecret.status, secret_environment_body_sha256: digest(settingsTokenSecret.body), secret_repository_status: settingsTokenRepositorySecret.status, secret_repository_body_sha256: digest(settingsTokenRepositorySecret.body), secret_organization_status: settingsTokenOrganizationSecret.status, secret_organization_body_sha256: digest(settingsTokenOrganizationSecret.body), body_sha256: digest(settingsTokenIdentity.body), verified: false }, oidc_subject_restricted: false, verified: false, blocked_reason: 'CAPTURE_GENERATION_FAILED' }
  blocked.content_sha256 = digest({ ...blocked })
  capture = blocked
}
writeFileSync(process.env.V5_SETTINGS_OUT || 'github-deployment-settings-capture.json', JSON.stringify(capture, null, 2) + '\n', { flag: 'w' })
if (!capture.verified) process.exitCode = 1
