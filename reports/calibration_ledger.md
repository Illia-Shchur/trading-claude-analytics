# Calibration Ledger

Running history of framework calibrations — one line per run, newest last. Each line: date · framework(s) · corpus window · adopted/rejected (+withheld/unadjudicated if any) · prior-tune re-validation tally · headline change · caveat. `meta` entries record changes to the calibration skill itself. This file feeds the next calibration's `PRIOR_CALIBRATIONS` block — keep it accurate, and never include it (or the retrospectives) in a report corpus.

- **2026-06-11** · fallen-knives + flying-rocket · 19 reports, 2026-05-14 → 2026-06-10 (BTC/ETH/Gold/SOL) · **11 adopted-with-modification / 17 rejected** · prior-tunes: n/a (first calibration) · headline: Deep-Value Override re-anchored (8% + fresh lower-low + worsening-flows veto) and throttled (quarter-size in downtrend, ≤1 unlock/5d, ≤25% cap); probability grid flattened (Rally 50→38 at scores 15–17); compound thesis stops (weekly close AND score <12) placed below the deepest buy zone with a per-report coherence check; computed companion score mandatory (eyeballed estimates prohibited); FR received tightening-or-neutral mirrors only, dangerous mirrors deliberately withheld (Hard Rule 6) · memo: `strategy_retrospective_20260611.md` · N=1 — re-validate after next full cycle. *(Entry backfilled 2026-07-02 when this ledger was introduced.)*
- **2026-07-02** · `meta` — framework-calibration skill itself · evidence: Jun-11 retrospective + revision logs + template audit; no market workflow run · headline: closed the self-validation gap — added a prior-tune re-validator (adopted tunes graded validated/harmful/not-exercised out-of-sample; rejections enforced against re-proposal; untested predictions re-resolved), a pre-apply audit of the whole adopted set (mutual consistency, reachability, throttle, decoupling, threshold crossings, scope) as the last gate before auto-apply, per-framework×asset grading (FK and FR series no longer blended), skeptic panels with strictest-wins merge, deterministic corpus parsing from filenames, loud coverage disclosure for dropped reports/unadjudicated tunes, incremental corpus windowing, and two new hard vetoes (CLAUDE.md Hard Rules out of edit scope; no reversal of prior adopted tunes without a `harmful` out-of-sample grade) · the new machinery is itself untested until the next calibration run.
- **2026-07-04** · fallen-knives + flying-rocket · 39 reports, 2026-06-11 → 2026-07-02 (BTC/ETH/Gold/UNI) · **38 adopted-with-modification / 41 rejected (+1 withheld at pre-apply audit, 0 unadjudicated)** · prior-tunes: 15 validated / 4 not-exercised / 0 harmful (all 17 prior rejections held) · headline: FK-side data-integrity + stop-discipline + EV/scoring hardening (metric-history continuity, sentiment provider pinning + boundary tie-break, gold/low-vol asset adaptation, Stop Migration Ledger, re-scoped buy-zone coherence check, terminal-vs-extreme reconciliation, gate reachability disclosure); FR-side corrected an inverted funding-carry sign convention + zero-floored carry income, decoupled the squeeze-trap penalty from its OI conjunct, added a §7 Cover-Trigger Preflight veto, symmetrized the Verdict-Confidence Collar, and ported the single-observation durability lock (new Principle 13) — all FR tunes untested pending FR's next live report (zero FR reports since 2026-06-18); surfaced but NOT fixed this cycle: the Deep-Value Override and its worsening-flows veto are correlated in time (Override can only fire in a slow grind, never a sharp V-reversal) — flagged for next calibration · memo: `strategy_retrospective_20260704.md` · N=1 — re-validate after next full cycle, doubly so for Flying Rocket (zero live reports exercised these tunes).
- **2026-07-10** · `meta` — framework-calibration skill itself · evidence: owner directive (workflow cost explodes as the report corpus grows — the v1 design ran one agent per report and one skeptic panel per tune, ≈125 agents on a 90-report month); no market workflow run · headline: lean v2 of `backtest-workflow.template.js` — extraction chunked (one agent per ~6-report same-series chunk, ceiling 8; identity still code-joined, chunk failures loud), 7 diagnosis dimensions merged to 4 (graded-results payload no longer duplicated 7×), a name-level triage dedupes near-duplicate tunes before verification (code rebuilds the kept list from originals, so triage can drop nothing silently), skeptic panels batched ~5 tunes/agent EXCEPT capital-deployment tunes which keep per-tune solo panels (money-moving tunes keep undivided scrutiny), compact JSON payloads throughout, stale macOS default paths fixed · coverage guarantees unchanged: every report extracted, every prediction graded, every tune adversarially refuted, strictest-wins merge, unadjudicated ≠ adopted, loud coverage disclosure — ≈30 agents where v1 ran ≈125 on the same corpus · the lean machinery is itself untested until the next calibration run.
- **2026-07-10b** · `meta` — deterministic toolchain (`tools/`) adopted across all three skills · evidence: owner directive (certainty in calculations, data fetching, performance consistency) + the audit's recurring hand-arithmetic failure modes (4-report RSI NOT-FOUND debt, ETH ceil(7/9×8)=6 misprint ×3 reports, eyeballed EV sum-checks, ADR half-session absorption, "estimated" 200-week MA) · headline: `fetch.mjs` (live numeric backbone: CoinGecko/Yahoo/alternative.me/FRED, cross-checked spot, computed weekly Wilder RSI-14, exact 200-week SMA + gate-6 ±8% boolean, ADR sessions, F&G gate-1 streaks, FR 1y-ATH cycle-cap input), `compute.mjs` (rubric band classifiers mirroring the SKILLs letter-for-letter — FK edges → higher band, FR edges → LOWER band per Hard Rule 6; ceil thresholds; per-asset rounding; EV sum-check; stop coherence), `lint-report.mjs` (mandatory ```json machine``` block schema report-machine/1 validated after every save, before every commit; FR lint errors on missing price/time stops and >50% book), `selftest.mjs` (regression vectors incl. the ceil misprint) · calibration integration: extraction agents treat machine blocks as authoritative, selftest runs before every workflow · coupling rule: SKILL band changes must change lib.mjs+selftest in the same commit · scope honesty: ETF flows/on-chain/COT/news stay live web fetches (Farside bot-blocked) · no band, threshold, phase size, or stop moved · untested until the next live report exercises it.
- **2026-08-05** · flying-rocket · 4 reports — the framework's entire non-crypto track record, 2026-06-16 → 2026-08-04 (Gold ×2, SPX ×2) · **6 adopted (5 with modification) / 1 rejected (+0 withheld, 0 unadjudicated)** · prior-tunes: 4 validated / 1 harmful-in-effect / 1 not-exercised (the Jul-04 "fixed non-crypto gate schema" mandated a fixed schema and specified no mechanism to fix it — four reports produced four denominators; repaired here, not reversed) · headline: **restrictions formalized, coverage refused** — a restriction-only "Adapted Non-Crypto Reads" annex freezes the gate schema per class (N/A {4,6,9} → denominator 6, linter-enforced), caps the ATH-distance valuation fallback at 3 unless a pinned long-run measure (equity → Shiller CAPE; metals → CPI-deflated real price) is ≥90th percentile of ≥50y, pins rounding (spx/ndx half-down, no longer report-overridable), resolves untransferable crypto-native thresholds to NOT MET on calendar days, declares Channel B unavailable and Phase 3 unreachable (ceiling 18 < 19), and prints the squeeze-trap penalty as a MISSING PROTECTION rather than `penalty: 0`; the SKILL `description`/scope/Asset-Generalization table are **unchanged** and CLAUDE.md's FR scope line is surfaced as an owner decision, never edited · **most important rejection: the forward-P/E valuation substitution that motivated the run** — live data shows forward P/E 19.6 (≈ its own 5y/10y averages) and CAPE 41.9 (~29% above long-run average, near a 150y record) are BOTH true, the MVRV-Z analogue is CAPE, and forward P/E would have scored the leg 1 at the Oct-2007 top while capping the equity ceiling at 12 against unlock lines of 11/13; the motivating premise (leg over-credits by ~4 points) is **not supported** and the 2026-08-04 leg is recorded as correctly scored · graded: both gold stand-downs CORRECT (gold −0.77% over 7 weeks vs a +3% minimum-edge filter; Jun-18 EV_price 2.48% too bullish, modal band missed), both SPX reports UNTESTED (one day of path) · corrected in the record: sp500_..._1642 gate 7 ✅ was unearned ($77.6B/181 calendar days = $429M/day, fails the >$500M bar) → 2/7 gates, Distribution leg 0, mechanical 7; verdict unaffected · surfaced NOT fixed: the `ceil(legacy/9 × active)` conversion has flat spots (1B is flat 6→7; active 8→9 is flat at all four phases) — general, affects the crypto path too · toolchain co-landed: `frThresholds()` (the FR converter — `ceilThresholds` is FK's, p3 7/9 vs FR's 8/9), `FR_NONCRYPTO_NA`, `ROUNDING` pins, and two previously-unenforced linter paths (`gates.na` content, pinned-rounding override); epoch 2026-08-05 so no existing report is retroactively broken · memo: `flying_rocket_calibration_20260805.md` · N=1 and weaker than usual — 4 reports, **0 trades, 0 realized P&L** on this class, and FR is still N=0 on realized money on its native class; re-grade at the first non-crypto report that reaches a non-stand-down score.
- **2026-08-05b** · fallen-knives · 49 reports, 2026-07-04 → 2026-08-05 (BTC 18 / ETH 18 / GOLD 12 / SOL 1) — the window since the Jul-04 FK calibration · **1 adopted-with-modification / 5 rejected (+0 withheld, 0 unadjudicated)** · prior-tunes: not re-graded this pass (scope was three named §9.2 blind spots, not a full re-validation) · headline: **three blind spots tested, all mechanically real, all zero realized cost — and the one clause that shipped came from a surface none of them pointed at.** Adopted the **Entry-zone ratchet** (§6, entry-side, restrictive): an unlock authorizes a tranche inside its named zone only, a re-anchor is not an unlock, downward re-anchoring runs the coherence check + D6 exception 1, upward re-anchoring needs ≥5 consecutive closes above the shelf (a floor, never a ceiling on caution) + zone top below spot + a void condition + may never raise the number the coherence check runs against; **the Deep-Value Override is exempt** — its trigger is anchored to the last fill's blended cost, not the next zone, so an 8%-below-basis firing routinely prints *between* zones and the clause as originally proposed would have blocked it, re-creating exactly the unreachability the Jun-2026 re-anchor removed ($59,110, −9.1% off a $65K basis, score 16 / F&G 9). That defect was caught at the skeptic panel, not at proposal · codifies thrice-exercised practice (ETH Jul-2→Jul-10, BTC Jul-2, gold Aug-5) that lived nowhere in the SKILL; family-resemblance to the Jul-04 "zone-freeze/band-anchor-lock" rejection is **disclosed and negated** (downward moves permitted, stop input may not rise, the Jul-2 re-stage is the practice being codified) — not a reversal · **most important rejection: the weekly-vs-daily RSI divergence**, both constructions — the spread version is anti-correlated with weekly RSI *by construction* so it bites hardest at maximum washout and would knock the score below the Override's mechanical ≥15 arming bar at the lows (this framework's own documented past failure), the absolute version fires 2 of 19 reports both already gate-blocked, and out-of-sample over 2y × 4 assets the gradient is **non-monotone with three sign changes** with per-asset signs disagreeing (ETH +7.40%, SOL −1.89%) while **gold has exactly one qualifying day in two years — 2026-08-05 itself** · also rejected: a breakout/volume leg or gate (anti-gate has no mechanism; leg-level double-keys volume with capitulation-(a)/gate 7), a negative drawdown tier (~9pp from firing), a macro leg (composite max 20→22 silently loosens ~17 absolute thresholds), a signed gate 9 (adds gate credit = loosens unlocks on the long side) · **new structural finding: gold's deep tranches are SCORE-bound, not gate-bound** — gates 3 and 6 need −34.4% and −27.6% moves so the board ceilings at 6 of 8, and with the sentiment leg permanently pinned at 2 the composite ceilings near 13 at −30% drawdown and 15 at −50%, making Phase 2 barely reachable and Phase 3 unreachable absent a catastrophic decline; this is the better reason all three score-*lowering* tunes fail · **dispositions corrected: items 1 and 3 are "insufficient evidence", NOT closed** — zero realized cost over one month with **zero fills** measures absence of exposure, not absence of defect; named re-test triggers recorded · item 2 closed on mechanism (gate 9's predicate is asset-relative and *was* resolved differently for gold vs BTC on 07-13; D1 is the signed macro channel), but a live corpus contradiction is referred forward (gold 08-01 declined a −0.5 D1 as double-counting "the Fed stance already keys gate 9"; gold 08-05 took −1.0 with macro as factor (ii) — D1(a) bars re-weighting what a **leg** scores, and gate 9 is not a leg, so 08-05 is right) · toolchain co-landed, no rubric effect: gold sentiment fallback becomes a **measured** conclusion (GVZ and PHYS premium both backtested over 10y and rejected — GVZ no gradient and direction-blind; PHYS split-half inverts, tail 3.67% vs a 3.59% unconditional baseline, effective N≈16 — both now unscored `context.sentiment_proxy`), `ath.all_time_verified` discharges the "10y window high" debt line, `proximityPanel()` closes §9.2 item 4, and `completedCandles()` fixes a weekly-bar artifact that had moved gold's momentum band 2→1 · **process disclosure: the packaged `Workflow` was unavailable; the adversarial structure was run with individual agents (3 diagnosers → 1 adversarial reviewer tasked with attacking the unanimous null → 1 solo skeptic panel + pre-apply audit), and full Extract/Grade over all 49 reports was not run as separate passes. All three diagnosers returned ZERO tunes; the adversarial step produced the entire adopted result — had the pipeline stopped at consensus it would have shipped nothing and recorded a false "no defect found."** · memo: `fallen_knives_calibration_20260805.md` · N=1 — one clause with zero in-sample decision impact, an N=1 constant, and no linter enforcement; re-validate after the next full cycle and specifically after the next report that actually fills a tranche.
- **2026-08-05c** · `meta` — framework-calibration skill itself · evidence: user-directed review of the skill's own track record (this ledger, the 2026-08-05 and 2026-08-05b memos, the target SKILLs' revision logs) — no market workflow run · headline: **the pipeline's two live failure modes, made structural, plus a measured extraction-cost cut.** (1) `Workflow` unavailable in 2 of the last 3 real invocations with no documented fallback (2026-08-05b lost Extract/Grade as separate passes over all 49 reports) → new "When `Workflow` is unavailable" section codifies the degraded procedure (preserve the phase barrier, the code-side identity join, strictest-wins vote merge, solo capital panels, the null adversary) and requires the ledger line to say so explicitly. (2) A unanimous zero-tune consensus walked straight to Synthesize in v2 — exactly what happened on 2026-08-05b, where all three diagnosers found nothing and a bolted-on adversarial step produced the entire adopted result → the template now runs a **null adversary** on any dimension (or whole run) that returns zero tunes, as a first-class phase step, not an improvisation (new Principle 9). (3) Extraction re-read the ` ```json machine ` block and the "Verified Live Data Points" section with an LLM despite both being deterministically parseable — new `tools/calib-corpus.mjs` slices both out (drop-list matched by heading TEXT, fail-open on any mismatch, byte-reconciled) and projects the machine block into a `.digest.json` that's joined in code, exactly as identity fields already were; measured **31.9% byte reduction, 0 reconciliation failures** on the 49-report 2026-07-04→2026-08-05 FK window. (4) The prior-rejection list was re-derived by an agent from prose memos every run and fanned into every downstream prompt → new `reports/calibration-registry.json` + `tools/calib-registry.mjs` (append-only, schema-validated, keyword-overlap `match` as a pointer not a verdict), backfilled with all rejections (and, where the ledger named them individually, the adoptions) from all four real runs to date. (5) Realized P&L was never read despite Principle 6 mandating it → `tools/position.mjs all --json` is now a mandatory `args.position` input to Grade, read via `.band` with an explicit "no realized evidence" statement on EXPIRED/missing, never an inferred zero. (6) A SKILL edit could ship desynced from the `tools/lib.mjs` it mirrors → new toolchain-coupling guardrail + pre-apply check + `toolchain_edit_required` field; a tune is withheld, not applied, if its coupling doesn't land in the same commit. (7) The template's own hand-fill burden (typing `REPORT_FILES` from `ls reports/`, hand-copying `PRIOR_CALIBRATIONS`) is gone — the workflow now takes `args` built from the four tools above, the same pattern `~/.claude/workflows/poker-hand-analysis.js` already uses. (8) Fixed the skill's own duplicate Principle "7." numbering — the exact defect class its own apply-time validation pass exists to catch — and three declared run modes (`full`/`scoped`/`meta`) replace "always exhaustive," so a scoped run (like 2026-08-05b) states what it skips instead of surfacing it only in memo prose · verification: `tools/selftest.mjs` passes (17 new vectors: byte reconciliation, fail-open on a non-matching heading/unparseable block, registry schema validation, keyword-overlap matcher); the rewritten template was run end-to-end under a stubbed-agent harness against a real 8-report slice (all 5 phases completed, the null adversary correctly fired for all 4 zero-tune dimensions, the missing-`position` warning fired) — this is a wiring check, not a live run, and the new machinery is untested under real agents until the next market calibration · no change to any Fallen Knives / Flying Rocket rubric, band, threshold, stop, or cap.
- **2026-08-06** · `meta` — framework-calibration skill itself · evidence: `Workflow` unavailable in this session too (2 of the last 3 real invocations, per the 2026-08-05c entry above); measured corpus statistics on the real 60-report 2026-07-04→ window; no market workflow run · headline: **replaced the `Workflow` dependency with a real execution engine, and closed out the compression question with a measurement instead of a guess.** (1) New `tools/calib-run.mjs` — a file-routed run driver with real filesystem access (unlike the `Workflow` template) and no agent access: it plans one PROMPT FILE per task, the calling agent loop spawns one `Agent` call per task and writes the JSON result to the path named, replying with only a one-line `OK`/`FAIL` status — payloads never enter the orchestrating conversation's context, cutting main-loop cost from an estimated ~200 KB of accumulating intermediate payloads (the improvised 2026-08-05/2026-08-05b fallback) to on the order of a few thousand tokens of orchestration for a ~60-agent run. The phase barrier, the null-adversary gate (Principle 9), strictest-wins vote merging, solo capital-deployment panels, and coverage-disclosure accounting are now enforced by exit codes (`plan <phase>` refuses while the prior phase isn't `collected`), not prose discipline — and a missing/malformed agent output is **resumable**: `collect` names only the failed `task_id`, so one re-spawned agent recovers the run instead of losing it. `backtest-workflow.template.js` remains valid if `Workflow` is ever available but is demoted to a non-reference path that can drift, since it cannot import this file's logic. (2) **Investigated and rejected: report-level compression of "unchanged" content.** Measured on the real 60-report window (2026-07-04→2026-08-05, both frameworks): 0 of 60 reports had an identical score/gates/tranche fingerprint to their immediate predecessor — every report moves a gate, the score by >1, or a tranche. This was the single largest hoped-for cost win and it does not exist in this corpus; documented as rejected-on-evidence rather than left as an open question. (3) In its place: `tools/calib-corpus.mjs` gained `--max-per-series N` (default 12), an event-preserving cap (a gate flip, tranche/stop change, >1-point score move, or unlock-line crossing is never dropped to satisfy the cap — verified non-binding on today's corpus, since every report in it is an event; it bounds a future higher-frequency window, not today's) and a third section drop (`<Framework> Composite Score`, ≈7% of post-slice bytes — every leg value is already in the digest) — **measured 42.1% total byte reduction, up from 31.9%.** (4) **Model routing**: Extract now runs on `haiku` (pure schema-bound transcription against an already-parsed digest, ~60% of a run's agent-input bytes), Grade on `sonnet`, Diagnose/null-adversary/Verify/Synthesize on `opus` — the reasoning tier reserved for the reasoning that matters. (5) `tools/selftest.mjs` gained orchestration vectors testing the driver's pure functions directly (`isEventReport`, `selectWithCap`, `zeroTuneDiagnoses`, `mergeStrictestWins`, `applyTriageClusters`, `validateSchema`) rather than only the file-based state machine around them · verification: `node tools/selftest.mjs` passes (new vectors above); a full 5-phase dry run with stubbed agent outputs completed end-to-end on a real 8-report corpus slice, including forcing all 4 diagnose dimensions to zero tunes (confirmed all 4 null-adversary tasks fired and `plan verify` refused to proceed until they were collected) and a deliberately-deleted single output file (confirmed `collect` failed naming only that one `task_id`, not the whole phase) — this is a wiring check under stubbed agents, not a live run, and the new machinery is untested under real agents until the next market calibration · no change to any Fallen Knives / Flying Rocket rubric, band, threshold, stop, or cap.
- **2026-08-06** · fallen-knives · run mode **`full`** · model map: default (Extract=haiku / Grade=sonnet / Diagnose+null-adversary+Verify+Synthesize=opus) · 49 reports, 2026-07-04 → 2026-08-05 (BTC 18 / ETH 18 / GOLD 12 / SOL 1); corpus 42.1% byte reduction, 0 warnings, 0 `sampled_out`, byte reconciliation OK on all 49 · **5 applied (all adopted-with-modification) / 4 rejected / 2 withheld at pre-apply / 0 unadjudicated** (12 proposed → 1 triage merge → 11 panelled) · prior-tunes: **16 validated / 20 not-exercised / 0 harmful** across the 36 nameable mechanisms of the 2026-07-04 set — its **first out-of-sample test**, because the 2026-08-05b run was `scoped` and skipped re-validation entirely; all 18 FR-side tunes are not-exercised because **Flying Rocket produced zero live reports in the window**, extending its silence to two full cycles · scorecard: **117 graded predictions — 38 correct / 20 partial / 37 wrong / 22 untested** · headline: **the risk discipline held and the forecast layer did not, so the forecast layer is now graded.** Applied the §4 **Score-line vacuity & binding-constraint audit** (score-axis mirror of the gate-reachability disclosure; attainable ceiling re-derived per report, every governing score line tagged LIVE/VACUOUS-FALSE/VACUOUS-PERMISSIVE/VACUOUS-BLOCKING against *the score it actually reads*, binding axis named, one-directional, **Deep-Value Override exempt** so a vacuity tag can never bar its ≥15 arming line), **§5 EV sign attribution** (the trailing-2-week contradiction disclosure was one-sided and named only the bullish failure mode — the bearish one is what ran; negative EV now decomposes per scenario and states whether probability weight or band distance carries the sign, geometry-driven reads size **downward only**, plus a non-dissolution clause so the label cannot dissolve the EV-floor check, the collar or terminal-vs-extreme reconciliation), **§5 EV Calibration Line** (the framework grades its analyst via D7 and had never graded itself: ≥5-distinct-report-date same-sign streak contradicted by realized price forces an on-record systematic-bias statement, default consequence is **demotion of EV to corroborative-only** which cannot then lift the Verdict-Confidence Collar; band re-derivation requires a cited realized trend-structure event and does **not** reset the streak — a bundled pair whose unbundling re-opens a bounce-chasing hazard), a **discharge clock on the standalone-FR obligation**, and **expiry of the frozen FR-coverage claim** · **most important omission (corrected 2026-08-07, see the 2026-08-07 entry below — narrower than first framed):** BTC narrated a live filled "10% Phase 1A @ ~$65K" and mark-to-marketed it across **nine** consecutive reports, ETH narrated "HOLD ~5% @ ~$1,844" across **seven**, while the owner-attested ledger shows dust in both and **no FK deal tag anywhere**; the two tunes written for it were both **withheld at pre-apply** because they legislate the same object with **incompatible state machines** (keep the tranche `deployed:true` with its stop and LIFO ordering vs retire it to UNFILLED with `deployed_pct: 0` and no stop anchor), neither ships alone, and both carry heavy toolchain coupling that could not land this commit — **the next run must adjudicate the two designs against each other before panelling either** · most important rejection: the **compound-stop score-axis degeneracy disclosure**, killed on a false premise — it claimed the SKILL is silent on the continuously-satisfied score axis while §5 legislates exactly that case, and quoted it with the **polarity inverted** (the score key is a *brake* on a conjunctive stop, so a continuously-satisfied key makes the stop fire *more* readily); a load-bearing figure was also unreproducible · also rejected: regime-relative gold COT bar (self-referential), Key-Trigger decay (would have retired gold's real-yield channel exactly when it mattered), renumbering the 2026-07-04 log (the 36-vs-38 discrepancy is real and is recorded, not silently edited into the past) · toolchain: **zero `lib.mjs` edits owed** — no applied tune moves a band, threshold, cap or rounding rule; all five ship as **prose discipline the linter cannot grade**, which is a stated weakness (recommended zero-schema-cost follow-up: compute per-scenario EV contributions inside `evCheck`) · **process disclosures, both real:** (1) a **driver defect found and fixed mid-run** — `calib-run.mjs` derived the re-validation window from `max(priorCalibrations.date)`, so the later `scoped` (08-05b) and `meta` (08-06) entries pushed the boundary past every report and handed the re-validator an **empty series**; fixed by anchoring to the corpus window start (new exported `postCalibrationBoundary`) with 6 paired selftest vectors — uncaught, this run would have reported "all tunes not-exercised" as a *finding* rather than a bug; (2) the **first ETH grader fabricated an infrastructure defect** (claimed its prompt held BTC digests and pointed at a GOLD output path — both verified false) and returned an implausible 7/1/0-wrong/7; it was discarded and re-run, attempt 2 returned 12/4/**10**/6 and independently flagged attempt 1's leniency · memo: `fallen_knives_calibration_20260806.md` · **N=1, and weaker than usual on deployment: zero fills in all four covered assets and zero FK-tagged deals all window, so deployment quality is measured against an absence of exposure, not outcomes;** the ≥4 (vacuity audit) and ≥5 (EV Calibration Line) persistence constants are unvalidated point choices whose divergence is flagged rather than harmonised — re-validate after the next full cycle and specifically after the next report that actually fills a tranche.
- **2026-08-07** · correction (not a run — no market workflow, no agents) to the 2026-08-06 entry above and its memo/SKILL revision-log entry · evidence: owner directive ("ledger is correct here those report were done before ledger existed") + direct read of `btc_fallen_knives_20260803_1411.md`, `btc_fallen_knives_20260805_1008.md`, `eth_fallen_knives_20260805_1008.md`, and `lint-report.mjs`'s `ENTRY_PRICE_EPOCH` block · **the 2026-08-06 run's headline defect — "the largest defect this run found is documented and NOT fixed" — was overstated.** Both narrated fills (BTC "10% Phase 1A @ ~$65K", ETH "HOLD ~5% @ ~$1,844") predate Hard Rule 8 and the position ledger entirely; the owner attests on record that neither was ever real; and three independent mechanisms had already closed the *reporting* side of this before the 08-06 calibration ran — Hard Rule 8 (2026-07-28) made the ledger the position of record, the 2026-07-29 `ENTRY_PRICE_EPOCH` makes `lint-report.mjs` **error** (not warn) on a future prose entry that reads like a fill, and the 08-03 (BTC) / 08-05 (ETH) reports had already retired the narration by hand — explicit Position Reconciliation sections, `deployed_pct: 0`, UNVERIFIED in both directions, no stop anchored, no capital sized against it. **What is genuinely still open, and costing real deployment:** `basis.reliable = false` on both assets (5 unbacked BTC disposals; 24 unbacked ETH disposals, 8.5064 ETH — the account's largest gap) means the ledger can neither confirm nor refute the narration, and a genuinely-unlocked Phase 1A has sat unfunded for **three consecutive reports on each asset** as a result — a personal-accounting-ledger fill-ingestion defect, not a framework defect. The fix is backfilling the ledger's missing acquisition history (so `basis.reliable` goes true) and tagging future fills `FK-P1A`; with the owner's attestation now on record, both legacy narrated fills should read **UNFILLED** going forward. This also adjudicates the two tunes the 08-06 run withheld: the `Fill-corroboration clock` (decay an unconfirmed narrated fill to Hard Rule 4's default) is the design that matches what the 08-03/08-05 reports already did by hand and is the one the next run should panel first; the sibling `LEDGER-CONFIRMED vs UNVERIFIED` design would have left the block in place indefinitely. No score, band, threshold, stop, cap or Hard Rule 6 non-negotiable touched. Memo §0 (`fallen_knives_calibration_20260806.md`) and the FK SKILL revision-log entry carry the same correction, dated in place rather than edited into the past.
