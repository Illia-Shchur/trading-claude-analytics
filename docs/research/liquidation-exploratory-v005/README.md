# V005 — confirmed post-shock structure and daily context

Owner-authorized successor to exposed v004. This is DEVELOPMENT research on the same nine assets and decision window. Daily RSI-14 and the 200-day simple moving average are directional context for additions; first entries remain price-led. All original risk, capital, leverage and60-day limits survive.

The fixed seven-profile ladder is baseline → post-shock entry → H4 initial stop → refreshed invalidated staging pivot → RSI-only / SMA-only / both context overlays. S&P addition gates remain present in every profile. Context overlays are conjunctive addition gates, not scores or independent evidence. All outcomes are reported, with no winner selection or threshold search.

The new entry contract uses confirmed five-H4 swings formed after shock geometry, then two consecutive closes beyond a frozen swing level and an H1 retest of that level. It changes both branch confirmation and the entry reference; retaining the old pre-shock branch boundary would fail to test the owner's recovery example. The original daily/OI shock detector and original midpoint reversal target remain fixed.

Supplemental price warmup feeds context only. It cannot create earlier router events or change the original strategy cohort. Context missingness blocks enabled additions only. The precise timing, symmetric comparisons, candidate reset/invalidation and lifecycle rules are declared in exploratory-policy.json before outcomes. Existingv003/v004 policies, inputbytes, results and default codepaths stay unchanged.

One existing ambiguity remains separately disclosed: earlier reversal midpoint touches are not latched before confirmation in the inherited implementation. This study preserves that behavior rather than bundling an unrequested correction into the entry revision.
POST_SHOCK_ENTRY jointly changes routing, reference level and prefill invalidation. Seven profiles include the existing baseline alias and six new rule configurations; they are not seven independent hypotheses. All new context uses completed UTC days, and pending addition intents remain immutable during pivot invalidation.
