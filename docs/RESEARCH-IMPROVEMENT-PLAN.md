# Research improvement implementation plan

Date: 2026-09-05
Owner: user; implementation: GPT-5.6 Luna, xhigh; independent review: parent agent.
Status: packages A–F are implemented and parent-reviewed. The declared 8-asset physical computation and independent recomputation remain diagnostic and blocked by unresolved legacy lineage plus insufficient paired independent units. C's bounded inventory has an authoritative atomic fixed-attempt extension. D's v004 generator and shared fixed-evaluator adapter completed all 200 planned repetitions from the immutable executor; the result is a completed conditional synthetic diagnostic with every target unmet, so it makes no calibration or promotion claim. E has an additive typed numeric reconciliation command plus a governed completed-cycle rederivation boundary: input, label and execution roles are reopened and recomputed, and observed price conventions are explicit. No prospective cycle or observed-fill claim is made. The final clean-checkout reactor passed with 1,007 tests reported, zero failures/errors and one unavailable historical-fixture skip; the final clean-checkout reactor includes the compact-summary boundary.

## Outcome

Make it possible to finish a reproducible, interpretable crypto strategy experiment, measure the evaluator's reliability and cost, and collect forward evidence without weakening promotion or capital controls. A valid negative result is a successful experiment. No profitability target is an implementation acceptance criterion.

## Starting evidence and constraints

- `bin/analytics` only rebuilds a missing JAR. At audit time the JAR predated 55 Java source files; the launcher has no freshness check.
- The retained v5 registry contains physical base-data replay and missing-input receipts, but no completed strategy evaluation. Missing-input smoke runs are not failed configured experiments.
- All 32 legacy per-asset decisions include `POINT_IN_TIME_NOT_VERIFIED`. Preserve their original bytes and distinguish that limitation from economic failure.
- The frozen deleveraging precommit describes a fixed entry/exit/control experiment but leaves its numeric execution specification unresolved. Add a linked immutable specification; never overwrite the old precommit or reset its family exposure.
- Full default adaptive/null accounting estimates about 295 million attempts/evaluations. Synthetic benchmark speed is not production performance evidence.
- The current docs and skill mix generations and disagree with parts of the current implementation.
- The starting checkout is clean on `main`, 18 commits ahead of origin/main. Preserve all existing commits; do not reset, force-push, or publish those commits implicitly.
- Research remains crypto-only, spot baseline long-only, and no research path authorizes trading. Existing FK/FR stops, caps, ratchets and vetoes remain untouched.
- Existing v1-v5 content-addressed evidence stays immutable. New contracts must be additive or explicitly versioned. No fabricated PIT/fee/fill/custody evidence and no fixture promoted to real evidence.

## Work packages, in priority order

### A. P0: build freshness and actual executable identity

Implement a content-based build-input identity covering Java/resources, reactor POMs, Maven wrapper/config and relevant build inputs. Reuse the JAR only if it corresponds to those inputs. An untracked or mismatched JAR must be rebuilt or rejected; a failed build must not execute the previous artifact. Handle concurrent invocations safely, preserve argument quoting, and keep machine-readable command stdout clean.

Expose the actual executable SHA-256, build-input fingerprint, dependency/build identity and revision/dirty-source information in a version/build-identity command and new research provenance. Avoid self-referential JAR hashes and avoid claiming runtime source hashes identify a stale executable. Direct packaged execution must identify its compiled build, not silently adopt the current checkout's identity.

Acceptance: real launcher integration tests cover missing build, unchanged fast path, content changes with preserved timestamps, changed resources/POMs, failed rebuild, concurrent launch and paths/arguments with spaces. Provenance identifies actual compiled inputs and JAR.

### B. P0: executable fixed baseline and a complete physical experiment

Create an additive, linked exact baseline specification under the existing deleveraging family. Freeze parameters before inspecting baseline outcomes. Explicitly define completed 4h shock return/volume/volatility calculations, warmup, numeric thresholds, next executable child-bar entry, one stop and timeout policy within the precommit's horizon, adverse same-bar ordering, no averaging down, costs/filters, overlapping events and market-wide episode clustering. Record the rationale as a mechanism-based initial choice, not an optimized parameter.

Define matched downside controls using only information available at decision time, including the matching variables, historical pool, distance/calipers, exclusion/reuse rules, deterministic tie breaks and the same execution/lifecycle/cost treatment. Controls must not be selected using realized outcomes. Estimate paired event-minus-control results as well as unconditional returns and distinguish a price/volume proxy from direct observation of forced liquidation.

Use existing physical acquisition where valid; add/fix the minimum missing orchestration to finish the feature, opportunity, metadata, execution and portfolio chain. Use only declared inputs; unavailable optional derivatives/flow data must not block a spot-only premise. Keep retrospective USER_BOUND execution assumptions visibly non-activation evidence.

Acceptance: complete a real-data fixed-baseline run with immutable inputs, trades, controls, metrics, sample counts, dependence-aware uncertainty, portfolio reconciliation and clear disposition. Repeating the run reproduces semantic hashes. Record exact commands and runtime. If an external source truly prevents completion, retain a precise dependency receipt and finish all independent code and tests; never call that a completed economic test.

September 6 execution clarification: public acquisition exposed a source-confirmed Binance spot closure on September 29, 2021 (07:00–09:00 UTC). Preserve the original continuous-bar experiment and its failed receipts. Add an explicit, versioned, source-bound held-position closure policy before resolving the interrupted trades: no fabricated bars or fills, first reopening bar uses normal adverse gap handling, original wall-clock horizon and exact entry-boundary rule remain, and undeclared gaps still fail. Bind the amendment and its evidence to the run and regression-test the default and amended behavior separately.

### C. P1: explicit staged research and bounded computational cost

Provide a supported fixed-rule diagnostic path, a small frozen refinement path, and the existing full adaptive confirmation path. Prefer shared physical evaluator/statistical components over a parallel simulator. A fixed baseline must not require running a genetic optimizer merely to test one declared rule.

Every stage records its immutable search budget, evaluated behavior/attempt inventory, evidence phase, execution identity and promotion eligibility. Diagnostic passes cannot satisfy full SHADOW/confirmation requirements. Preserve cumulative exposure across reruns, refinements and legacy lineage; reject attempts to reset it. Budget exhaustion produces incomplete evidence, never a statistical rejection/pass. Do not reduce full adaptive acceptance thresholds or null budgets just to make it finish.

Acceptance: tests demonstrate no optimizer for the fixed path, exact frozen refinement inventory, stage-transition lineage, budget enforcement, deterministic resume and inability to promote diagnostic results. Benchmark a real physical baseline; clearly separate measurements from extrapolated adaptive cost estimates.

### D. P1: evaluator operating characteristics and useful failure explanations

Add an independently specified repeated simulation harness for no-edge and planted-edge markets. Produce market-like clustered/correlated episodes, fees, adverse execution and regime dependence; avoid testing an evaluator against its own formula or mocking the entire signal-to-outcome path. Freeze generators, seeds, budgets and acceptance targets before results. Measure false-positive rate and power with binomial uncertainty across plausible sample/effect sizes. Exercise the complete applicable stage's decisions, including selection when a search is performed; a fixed-stage validation cannot claim full adaptive calibration.

Reconcile representative trade math/portfolio outcomes against a small independent calculation. Existing Node parity and schema/security tests remain necessary but are not sufficient statistical validation. A failed power target must be reported, not tuned away on the same fixture outcomes.

Add additive diagnostic categories for INVALID_EVIDENCE, INSUFFICIENT_EVIDENCE, ECONOMIC_FAILURE, COMPUTE_INCOMPLETE and eligible/pass results while preserving canonical historical decisions. Report multiple simultaneous reasons and primary next action without disguising missing data as absent edge. Show raw trade count and independent market-episode count.

Acceptance: retain reproducible simulation results with uncertainty, stage scope and measured runtime; tests cover false-positive/power calculation, dependent samples, genuine planted/no-edge differences, independent math and disposition mapping. Do not assert evaluator calibration beyond measured evidence.

### E. P1: prospective observation and reconciliation

Inspect existing prospective orchestration and remote deployment evidence before assuming it is deployed or missing. Reuse existing protected publication, reservation and custody contracts. Provide an operational path for eligible frozen SHADOW candidates: decision before outcome, immutable event/source timestamps, idempotency/CAS, missed-bar recovery, gap/stale input rejection, expected-versus-observed signal/cost/outcome reconciliation, and clear sample-age/count reporting.

Collect public source observations independently where supported, but do not label retrospective input or an ineligible candidate as governed prospective evidence. If credentials, external approvals or eligible SHADOW evidence are absent, complete local implementation and tests, prepare exact deployment prerequisites and report the true blocker. Do not forge approvals, lower the trust root, activate live trading or place orders.

Acceptance: simulated clock and local integration tests exercise new bars, no-op, restart, duplicate events, outcomes too early, changed candidate, tampering, missing/stale data and reconciliation. A real cycle is required to claim operational deployment; local tests alone establish capability.

### F. P2: current operating guide and focused maintainability

Make one current operating guide and a capability/evidence matrix that distinguishes implemented, locally tested, physically exercised and remotely deployed. Mark historical commands as historical and link them without rewriting evidence. Update README, V5 guide, protocol and strategy-research skill consistently, including fixed versus adaptive stage requirements and present WFO behavior. Read the skill-creator skill before editing a skill.

Extract cohesive units touched by this work (build identity, baseline/control specification, stage policy, dispositions, calibration) from large orchestration/statistics classes where useful. Avoid a sweeping rewrite or schema churn unrelated to acceptance criteria. Keep deterministic math, I/O and orchestration separately testable.

Acceptance: commands in the guide execute or clearly identify prerequisites; no contradictory promotion claims; legacy read/validation regression tests pass; maintainability changes preserve behavior.

## Test and review gates

1. Run focused tests for each changed capability, including negative/tamper tests and actual CLI integration.
2. Run the complete Maven reactor test suite and required compatibility/schema checks. Record exact commands, totals, skipped tests, failures and resource/environment limitations; fix regressions before completion.
3. Run real physical baseline and reproducibility checks plus measured synthetic calibration. Keep raw/cache/bulk simulation data ignored, retain compact immutable receipts/results under appropriate versioned contracts.
4. Parent independently reviews the diff for build provenance, PIT/control leakage, lifecycle/accounting, multiple-testing exposure, stage promotion and prospective custody. Parent reruns selected meaningful tests and requests Luna corrections until resolved.
5. Update this plan with completed acceptance criteria, evidence paths and genuinely external blockers. No unchecked item is described as done.

## Implementation progress

### A — build freshness and actual executable identity: implemented and parent-reviewed

- `tools/build-input-identity.sh` fingerprints Java sources/tests, all module POMs, top-level compiled schemas, Maven wrapper/configuration, build tooling and the actual Java version/vendor/runtime descriptor. It uses quoted Git pathspecs for staged and unstaged dirty-source detection, including source paths containing spaces.
- `analytics-cli/pom.xml` emits the marker at package time. `BuildIdentity` reports the compiled marker, outer executable JAR path/kind/SHA-256/bytes, runtime and toolchain fields. Historical V5 Node-oracle `CODE_SHA` constants were preserved.
- `bin/analytics` performs content-based freshness checks, clean locked rebuilds outside `target/`, stale/empty lock recovery, Maven-from-repository execution with build output on stderr, and fail-closed post-build verification. It releases its lock before `exec`.
- Boot nested-JAR repository resolution was added so direct `java -jar` from another working directory can initialize the existing command surface and report the outer JAR identity.
- Additive schema and command-registration test expectations were updated for the versioned research schemas and the build-identity command. Historical schema IDs and evidence bytes remain unchanged.

Validation evidence from this checkout:

- At the A checkpoint, `./mvnw test` passed across analytics-contracts, analytics-core, analytics-infrastructure, analytics-market-data, analytics-reporting, analytics-research, analytics-cli, and analytics-compatibility-tests; the reported summaries included 54 compatibility tests and 88 analytics-cli tests with zero failures/errors/skips. A fresh full-reactor run remains required after the subsequent B–F changes.
- `./mvnw -q -pl analytics-cli -am clean package -DskipTests`: passed. The packaged `META-INF/build-identity.properties` marker was present.
- `./bin/analytics build-identity` and `(cd /tmp && java -jar <absolute-exec-jar> build-identity)`: both reported `status=KNOWN`, `kind=JAR`, the outer JAR path, and SHA-256 equal to independent `shasum -a 256`; the compiled fingerprint matched the helper output.
- Launcher checks covered unchanged mtime-preserving reuse, a preserved-mtime schema content change, staged/unstaged source paths containing spaces, forced failed rebuild with empty stdout and no stale execution, concurrent callers sharing one rebuild/lock, and shell syntax. Parent’s independent review also covered missing/deleted inputs, `.mvn` changes, empty/dead-owner locks and argument paths with spaces.
- The remote deployment audit remains an external operational limitation: existing scheduled workflow evidence is dormant configuration, with no observed active forward cycle or complete deployment bundle. No prospective operation is claimed here.

Packages A–F are implemented. The declared physical baseline remains `BLOCKED`/`INSUFFICIENT_EVIDENCE` because legacy lineage and paired sample requirements are unresolved. The completed synthetic operating-characteristics output is a fixed-stage conditional diagnostic only: all four cells completed all 50 repetitions, every predeclared target was unmet, and no calibration, promotion or prospective-cycle claim is made.

Parent acceptance: source review, seven independent reviewer regressions, an isolated actual package/direct-JAR identity check, concurrent preserved-mtime schema rebuild and failed-build checks passed. Package A is accepted subject to final shared-checkout integration verification after subsequent work. Actual executable identity must also be bound into the new package-B research evidence.

### B/C — fixed baseline and bounded diagnostic stage: full physical pass retained; promotion blocked

This section retains the earlier slice milestone and the full declared computation. The full result is a completed physical computation with an evidentiary `BLOCKED` disposition; it is not a promotion or calibration claim. Historical slice bytes remain unchanged.

- `strategy-research/definitions/fk-deleveraging-absorption/v002.json`, `strategy-research/experiments/fk-deleveraging-baseline-v002/controls.json`, and `strategy-research/experiments/fk-deleveraging-baseline-v002/experiment.json` are additive typed definitions. The numeric shock, matching, 240-hour lifecycle, raw-entry/raw-stop slippage convention, one-open-position admission, prior-only downside controls, and lifecycle-overlap exclusions were frozen and content-hashed before the retained outcome was opened. The prior baseline bytes and historical family evidence were preserved.
- `StrategyFixedBaselineV5` is the supported fixed diagnostic route. It derives setup rows by reopening the authoritative 4h Parquet conversion manifest and checking partition hashes, row counts, complete cadence, completed-bar timestamps, all 31 dependency availability boundaries and the frozen trailing formulas. It selects controls before opening labels or child bars, removes used candidates before matching, binds each asset's contract/model/capacity receipts, and invokes the production `TradeLifecycleV5` with a physical trust token. Event and control books remain separate.
- The route records all eligible, admitted, skipped, unmatched and unresolved rows, merges overlapping lifecycle intervals into dependence units, and retains an account-currency ledger. The additive `portfolio-policy-v001.json` freezes $10,000 starting cash, eight notional slots, chronological one-position admission, and hourly samples from physical 1m closes. This amendment was frozen after the retained integration outcome and is disclosed as post-slice; it does not rewrite that result or predate it.
- Exposure uses the canonical family HEAD and a baseline-definition behavior alias. A same baseline-definition/dataset retry is idempotent; a missing HEAD is initialized only after the legacy-family migration boundary is checked, and recoverable legacy history cannot be reset to zero. New results bind the attempt identity, typed input hashes, packaged JAR identity, semantic hash, economic semantic hash and exposure custody.
- Existing HEADs are also audited against retained `strategy-research/runs` JSON and JSONL records. The full result records 28 normalized legacy-family matches, canonical cumulative K=1 and attempt K=32, labels the lineage `UNRESOLVED_LEGACY_HISTORY`, and blocks promotion without resetting the family. Legacy artifacts and byte hashes remain discoverable in the result's `legacy_exposure_migration.matches` array.
- `fixed-baseline-produce-signal-bars` and `tools/prepare-fixed-baseline-signal-bars.sh` provide the replayable setup producer. The public Binance 1m hydration attempt is retained as `strategy-research/v5-records/receipts/fixed-baseline-public-1m-8942b85eaad63c4dd84dbb3c09a5dd12424ff5aec74cc38b773d80d8248e841d.json`; its historical filter/PIT status remains `UNVERIFIED_DEVELOPMENT_ONLY`/`USER_BOUND_RETROSPECTIVE`.
- The signal role now carries a typed producer receipt and is rederived from the frozen Parquet manifest and partitions at evaluation time. A rehashed signal-role mutation was independently rejected as `PIT_SETUP_UNAVAILABLE`; caller-authored role hashes cannot substitute for the producer.
- The source-confirmed Binance spot closure is versioned as the retained v1 policy bytes plus an additive v2 erratum generator. The lifecycle accepts only typed, contract-bound policy objects; raw interval arrays, wrong venue/instrument, contradictory bars and a deadline inside the closure fail closed. No synthetic bars or fills are created.

Physical validation retained for this bounded slice:

- Verified setup source: `strategy-research/v5-records/data-raw-replay/parquet-31f85e351861f05edc66794ec57369c0a7fafe67e236676aab5c6cc7bec4cfc6.json` with root `strategy-research/v5-data/backfill-20260825-v7-final-local/parquet`; all eight 4h partitions were reopened and matched their recorded bytes/row counts. The producer emitted 87,656 rows across the eight declared assets.
- Public execution hydration fetched 14,400 contiguous BTCUSDT 1m bars from Binance in 15 requests for `2021-09-07T16:00:00Z` through `2021-09-17T16:00:00Z`. The physical lifecycle found the first adverse stop bar at `2021-09-13T14:18:00Z`; this verifies the OHLC stop convention and cost arithmetic, not an observed exchange fill.
- The retained first run is an explicitly scoped one-event BTC integration slice: `event_count=1`, `admitted_event_count=1`, `skipped_open_position_count=0`, `matched_control_count=0`, `unresolved_execution_count=0`, `trade_count=1`, and `independent_market_episode_count=1`. It reports `evaluation_scope=INTEGRATION_SLICE`, `full_experiment_complete=false`, `promotion_eligible=false`, and `INSUFFICIENT_EVIDENCE`; missing declared assets are ETH, SOL, BNB, XRP, ADA, LINK and AAVE. It is not a completed 8-asset/5-year experiment and no parameter was tuned from it.
- Independent economic reconciliation gives gross P&L `-59.990374806000084` USDT, fees `1.939688785394`, slippage `0.969844392697`, capacity debit `0`, and net `-62.89990798409108` USDT. The account ledger posts realized P&L at the 1m lifecycle exit, uses a frozen $10,000 starting cash policy and physical 1m close marks, and asserts ending equity minus starting equity equals net P&L. Two computations produced the same economic semantic hash; custody/runtime hashes remain separate.
- The full declared physical bundle is retained under `strategy-research/v5-records/evidence/fixed-baseline-full-declared-v004/`. v20 and v21 have identical economic semantic hash `1e4d63e3cabaeaaa71a09d2106580218c2c3b998693d88927d7050ca7777bb08`, with 130 events, 126 admitted event lifecycles, 3 controls, 0 unresolved executions, 129 trades and 28 independent clusters. The paired sample has 3 clusters, so `paired_analysis_insufficient=true`; the result disposition is `INVALID_EVIDENCE` plus `INSUFFICIENT_EVIDENCE`, with promotion false.
- A new accounted e11 identity was evaluated from the fresh reconstruction with amended executor SHA `5a780f162ca0f8592c8281d8b3212d4a64bc0e1f2d43cee1f7de6a1b8708b90f` and physical-input SHA `2407e9957ee38ca789483ee49b8120941956c939fedd75af129399b44f01a883`. The retained `fixed-baseline-e11-result.json` has byte SHA `334f3ffe07de95694735600bb66ff5c95fb988e825d718d6a33b9311327c9946`, content SHA `35f69ab55486b4669bb68e309a2b749c763bad3f5678e4181fe5fd4c9b1140ba`, status `BLOCKED`, and disposition `INVALID_EVIDENCE` plus `INSUFFICIENT_EVIDENCE`; it charged exposure attempt K=37 while cumulative K stayed 3 and fixed-pair custody increased to two. An independent role audit found 162 of 166 role hashes semantically equal to the historical bundle; four 1m roles differ only in the public source label, with two also having one close and two volume value changes. Independent Decimal/economic comparison found all 129 trade prices, quantities, costs, net P&L, final equity, and metrics unchanged; provenance-sensitive hashes differ, so this remains a separately identified diagnostic computation.
- Exact commands, executor SHA `ff277e85470d3fde87a4723c17f31f2e1581cc98dd26c94a27e02ba211c64d7e`, physical input SHA `4f5ac7a69e4543aa34c66fa7dcf215a1b0a1eaf9ebe5d7178f6da312a7ddfa40`, Parquet manifest SHA `31f85e351861f05edc66794ec57369c0a7fafe67e236676aab5c6cc7bec4cfc6` and independent recomputation evidence are in that README. The bounded D adapter is additive and uses generated roles only; it cannot replace the physical Parquet/PIT boundary or invoke adaptive search.

### C — frozen refinement inventory completed; adaptive confirmation remains untouched

- `freeze-refinement` is the supported bounded stage command. It accepts only the three predeclared FK-DELEVERAGING-V002 members, enforces `candidate_count=max_attempts=3`, `optimizer=NONE`, append-only predecessor/exposure bindings and no outcome opening. The durable receipt is downstream of the blocked baseline and cannot promote or reset the family.
- The exact input and plan are `strategy-research/experiments/fk-deleveraging-baseline-v002/refinement-input-v001.json` and `strategy-research/v5-records/evidence/fixed-baseline-full-declared-v004/refinement-plan-v001.json`. Their pre-run bytes froze an inventory and budget only; run-9 member results are retained separately under `refinement-results-v004/` as post-baseline DEVELOPMENT diagnostics and do not advance the family.
- The plan bytes froze the inventory before member outcomes. Run 9 subsequently evaluated all three members through the shared fixed evaluator as post-baseline `DEVELOPMENT` diagnostics; all remain `BLOCKED` with `INVALID_EVIDENCE` and/or `INSUFFICIENT_EVIDENCE`, with promotion false. Earlier run-6 bytes remain immutable. The authoritative exposure HEAD now carries validated `fixed_attempt_pairs` under the existing CAS/lock/atomic-write transaction, including one-time migration of the legacy sidecar; ordinary adaptive appends preserve those pairs. Twenty refinement custody/lineage reviewer tests, 27 Node-oracle tests and five property tests pass.

### D — fixed-stage operating-characteristics generator and shared evaluator

- `operating-characteristics-plan-v001.json` and v002 are retained as immutable earlier designs. The binding design is additive `operating-characteristics-plan-v004.json` (content SHA `63e17392714365810db25d52f382f5b9578a2cead9ee06b0335e4360b41eb235`): it specifies the 1m/4h grid, 31-bar warmup, 14,400-minute lifecycle, eight-asset assignment, common-factor and AR(1) asset noise, Markov regime transitions, 32 calendar clusters with 42-day spacing, 50 raw treated events plus 50 controls exactly 21 days earlier, explicit -9%/-4% setup shocks, exact effect units, five seed streams and replicate mapping, four cells of 50 replications, 32 event/paired independent-unit minima, natural-yield disclosure, and Wilson upper/lower-bound acceptance. The full budget is 288,000,000 generated minute bars with streaming and a predeclared 8 GB RSS/720-minute limit; an abort is `COMPUTE_INCOMPLETE`, never a reduced budget. Results must use the fixed `StrategyFixedBaselineV5` + `TradeLifecycleV5` + shared disposition path, with no toy statistic.
- `StrategyOperatingCharacteristicsV4` now generates the declared synthetic roles: 240 one-minute children aggregate into each completed 4h setup bar, the warmup state is cloned between treated/control rows, Markov calm/stressed paths use `.003/.08` transitions and `.00055/.0012` scales, paired assets share common-factor draws, asset noise is AR(1) with the frozen multipliers, and future volumes are lognormal. The generated bundle carries per-asset contract/model/capacity receipts and is passed to the same `StrategyFixedBaselineV5.evaluate` path, including outcome-blind controls, `TradeLifecycleV5`, account-currency portfolio reconciliation, clustered metrics and joint disposition. Synthetic output is explicitly `CONDITIONAL_FIXED_BASELINE_SYNTHETIC_DIAGNOSTIC`, `pit_valid=false`, `observed_exchange_fills=false`, and never appends the canonical family head.
- A source/JAR freeze is retained in `operating-characteristics-source-freeze-v004b.json` with generator source SHA `1927d082ab0af71d694bfda91ef76b3f96851d1be5d125ebc75ddf07f1fdf569`, executor SHA `dec4470d815d05f4eb56b871d4b07657745c06952ac802991e7b6b7a11e9d20c`, and receipt SHA `50fb0562cef8569ed784f7e3af07593edce93b716f68a2a7fb8d64e724ba3693`. It explicitly records the earlier WIP prefix exposure. The earlier post-freeze prefix result is `3e54472f8a94973a53a87de44c9363c87608421b69352ca1465b5e76191407a5`, `PREFIX_COMPLETE`, two replications with 10 events/10 pairs/8 independent units, and `INSUFFICIENT_EVIDENCE` with Wilson bounds unavailable. The original additive v004d freeze/result remain byte-stable at their original paths; the intermediate corrected lineage is retained under explicit `*-corrected` paths. The final additive v004e prefix uses `operating-characteristics-source-freeze-v004e.json` (content SHA `8fe7bb979f01c68b799f71577b538e04598969504b8ee49cb1e8a7d18fa7ffe7`), current generator source SHA `c804ca1405255c58195d3a06685a452c73188c9a58d275ec891a7741170befce`, executor SHA `783e9ebef5a5e706d0ed7fbb5d30aaf21ae7d4cfcfbb934a69556e21a9c4bf35`, and result `operating-characteristics-prefix-v004e.json` (content SHA `58f183b276f770aff22d086061f7cee05ba25344b44feb0716001349ba80646e`, byte SHA `5eeb2ece1a7de8ddde22e1754a651797606b27dd024d408d16b8445f364f321f`). Against the retained corrected4 pre-freeze prefix (content SHA `afe0d368612201fb98b506e15010810da873d8fed544d05520ca69a97e5dc4f4`, byte SHA `8f7dcf6b4c064ddf36bf1f2534dc1010b8e2e1b670a8ce6a597329dd3660824c`), all generator inputs, setup/control selections, metrics, dispositions, decisions and portfolio totals match; eight representative trades match in price, quantity, cost and net P&L with maximum Decimal residual `9.856661156040E-14`. Lifecycle hashes and mark points differ only under the conservative availability amendment. This is bounded prefix equivalence, not all-200-replication or byte identity; see `docs/RESEARCH-D-PREFIX-EQUIVALENCE-V004E.md`. The immutable freeze's prior-outcome list predates two v004d transition records; their omitted hashes and explicit paths are disclosed in `docs/RESEARCH-D-PREFIX-PROVENANCE-ERRATUM-20260906.md`. The full four-cell run completed from the copied immutable JAR with the frozen 8 GB RSS/720-minute resource contract. The raw result is `operating-characteristics-full-v004-dec4470d.json` (content SHA `36ecb2be8690c9bb1bd5f0cf14817df98aeaf7c5a86e53690f0b40e98a4b0a71`, byte SHA `1156b5e34e2dca2fec545f5059b61ceac2e066a7c824959d0fc603f08794a8cb`, 79,657,531 bytes): 200/200 repetitions are `COMPLETE`, with 2/50, 0/50, 14/50 and 45/50 positive decisions for NO_EDGE:0, PLANTED_EDGE:0, PLANTED_EDGE:0.02 and PLANTED_EDGE:0.04. Wilson 95% intervals are respectively [0.0110,0.1346], [0,0.0713], [0.1747,0.4167] and [0.7864,0.9565]; every target is false under the frozen rule. The parent independent audit checked all rows and 800 representative trades with maximum Decimal residual `1.3441744201100E-13` USDT. The display-only compact projection `operating-characteristics-full-v004-dec4470d-summary.json` contains all 200 row metrics/seeds/counts/decisions/hashes (content SHA `7df566f3f677f00e47d2535531469de2634bfcf97427a58edb0719e84fe12b3e`, byte SHA `b169ba9dcd65ba13bf27696ae7f5dafe79ae391c66e3467507ac9c40078e7bc5`, 610,039 bytes) and validates under `strategy-research-evidence-summary/1`. The terminal observation receipt `operating-characteristics-full-v004-dec4470d-exit-receipt.json` (content SHA `5802d0d6a99c73af8d38412a6744446555001c9c7b79cd3c2694b5ea64c327c7`, byte SHA `8d429015741796a94ab949dee70bf9c6c85c5b34d38eebe8593f18c6b3b10ffd`) records the observed process end without fabricating an uncaptured child exit code. The independent RSS monitor sampled 3,876 times and observed a 6,649,184,256-byte peak; internal runtime was 19,641.381 seconds and observed process elapsed time was approximately six hours including startup. The result is `CONDITIONAL_FIXED_BASELINE_SYNTHETIC_DIAGNOSTIC`, `pit_valid=false`, `observed_exchange_fills=false`, `promotion_eligible=false`, and `activation_authorized=false`; it is not adaptive calibration or physical market evidence. The current source adds a post-freeze guard that samples process RSS at most once per second, checks the deadline on every generation boundary, and records `RESOURCE_RSS_UNAVAILABLE`, `RESOURCE_RSS_BUDGET_EXCEEDED`, or `RESOURCE_WALL_DEADLINE_EXCEEDED` as `COMPUTE_INCOMPLETE`; the amended prefix exercised those guards without an abort. The retained generator source file is not a complete runtime source snapshot and cannot reconstruct the historical executor by itself.

### F — current operating guide and capability matrix

- `docs/RESEARCH-OPERATING-GUIDE.md` is the current command and evidence guide. `docs/RESEARCH-CAPABILITY-MATRIX.md` separates implemented, locally tested, physically exercised, and remotely deployed capabilities. The fixed baseline, refinement inventory, D design, legacy lineage limitation, and dormant deployment state are linked to durable evidence without rewriting historical commands or bytes.
- `tools/prepare-fixed-baseline-physical-bundle.py` is the durable fresh-scratch recipe: it validates the 130-row event inventory plus three selected controls, derives the signal role from the pinned Parquet producer, hydrates all 133 public 1m windows (with the typed Binance closure receipts), copies compact typed receipts, and delegates canonical hashing to the Java assembler. The guide no longer relies on transient `/tmp` role placement as the only reconstruction path. A relocated bundle receives a new physical-input identity and cannot silently reuse the historical exposure tuple. The completed e11 preparation is retained in `fresh-physical-reconstruction-e11.json`: 133/133 windows, 1,914,720 hydrated rows, no failures, physical-input content SHA `2407e9957ee38ca789483ee49b8120941956c939fedd75af129399b44f01a883`, and evaluation deliberately deferred because the relocated root is a new identity.
- `StrategyProspectiveOutcomeReconciliationV1` and the additive `strategy-prospective-outcome-reconciliation-input/1` plus `strategy-prospective-outcome-reconciliation/1` contracts provide the numeric paper-outcome boundary. It reports missing observed fills as `UNAVAILABLE`, preserves expected-versus-observed deltas, and rejects arithmetic or maturity inconsistencies. It does not authorize prospective activation or convert historical opaque outcomes into observed fills.
- The completed-cycle path also requires typed `strategy-prospective-numeric-label-source/1` and `strategy-prospective-numeric-execution-source/1` artifacts plus a separately hashed input file. The supported `strategy-research-v5 prospective-runner` command carries these flags through signal creation and mature outcome resolution; it recomputes the complete result before append and before an idempotent retry, rejects self-consistent rehashed numeric substitutions or changed outcome receipts, and distinguishes actual fill prices from reference prices with separate modeled debits. Historical opaque sources remain valid only for their existing custody role. Command-boundary tests cover signal→outcome, an older bar after a newer signal, identical retry, changed numeric bytes, changed receipt bytes and invalid source with no ledger mutation.
- The timing/reconciliation checkpoint was packaged as `strategy-research/v5-records/evidence/fixed-baseline-full-declared-v004/executors/analytics-current-20260906-e09-timing.jar` with SHA-256 `06ae9c89a90ddf6e899909875c69d2c4258d5423d1b6a6cd61a4d18e573a4bed`. Running that JAR from `/private/tmp` reported the same outer JAR SHA and `kind=JAR`; its focused command, typed-boundary and availability checkpoint was 19 tests with zero failures/errors/skips.
- The retained-artifact schema/index path is exercised by `ResearchSchemaRegistryTest` (12/12) and `RetainedFixedEvidenceSchemaTest` (1/1). Fresh public hydration transport checks are 4/4 for closure/no-overlap/cache handling and 3/3 for the local hydrator helpers. The parent final clean-checkout reactor passed (995 tests reported, zero failures/errors, one unavailable historical-fixture skip). The B/C interrupted/resumed physical replay also passed; the summary-schema boundary is included.
- The additive `lifecycle-timing-v001.json` erratum preserves the historical v002/D bytes while defining bar-open exit identifiers versus close availability. Fixed-book admission and account cash use `availability_time`; gap-open fills remain available at the open.
- Historical metadata audit also found the declared hashes in retained D plan v002 and v003 do not equal their Java canonical hashes. Their bytes remain immutable and are classified by `operating-characteristics-historical-hash-erratum-v001.json` as `INVALID_FOR_FREEZE_BINDING`; only v004 is a valid D binding. The closure notice wording is preserved with an additive `non-trading-intervals-erratum-v001.json` because the notice gives an estimated two-hour duration, while the exact reopening boundary comes from the symbol-specific archive receipts.

## Delivery

Deliver reviewable code, current operating instructions, exact frozen baseline specification, compact physical/simulation evidence, test results and remaining operational prerequisites. Do not create an ordinary FK/FR market report or invoke report auto-publication for this engineering work. Leave changes uncommitted for parent review unless the user directs otherwise.

### Final independent verification checkpoint

The parent final clean-checkout Maven reactor initially exited 0: 995 tests reported, zero failures/errors, one existing physical-null test skipped for its unavailable historical Parquet fixture. Seven Python hydration tests passed. After the retention-boundary tests were added, the final full reactor exited 0 with 1,007 tests, zero failures/errors and the same one historical-fixture skip. The schema corpus now contains 164 documents / 181 IDs after the distinct display-only evidence-summary contract and the narrow legacy fixed-attempt-ledger contract. Original E09 and post-amendment E11 JARs are immutable checkpoints, not a claim that later source changes are already packaged. Final package identity must reflect the final source. See `docs/RESEARCH-IMPROVEMENT-REVIEW.md` for interrupted refinement, corrected exit chronology, fresh-source drift and numerical equivalence.

### Final integration correction — explicit retention archive boundary

A final read-only audit found that the original tracked v5 corpus indexed successfully (11 records), while this work's mixed raw `evidence/` archive broke the default authoritative index. The final integration correction adds an explicit, versioned noncanonical archive boundary, keeps strict validation outside it, rejects canonical publication overlap and unsafe markers, and registers the preserved legacy fixed-attempt sidecar's read-only shape. No retained path or byte was moved or rewritten. The exact marker is `strategy-research/v5-records/evidence/.retention-archive` with SHA-256 `1f5a1a4f5f06632c8565a245d08b37f773741eabe83392e387ae968d6c789beb`; raw and partial JSON below it is excluded only after that marker is validated, while transaction controls remain strict. The archive and publication-control negative tests pass, and a fresh E14 package indexes the whole current root with 13 records (11 canonical records plus current HEAD and its fixed-attempt ledger), with index content hash `6a557327f21e21bb8f9aa0de48a9d9becb198289d3438ea58edf1349aed65686`. The current schema corpus is 164 documents / 181 IDs. The 995-test/E12 package and E13 index package remain earlier checkpoints; E14 is the package for this final index-boundary correction.

The E14 executable is retained at `strategy-research/v5-records/evidence/fixed-baseline-full-declared-v004/executors/analytics-current-20260906-e14-retention-index.jar`, SHA-256 `4567c7d493f07cdec806f046c40007e02bbac7358c41cd0eb5e1403183395a6d`, with build-identity receipt `analytics-current-20260906-e14-retention-index-build-identity.json` and source fingerprint `fd1599dc5b64828c52203f796deb6f884d1f32ec602a97454a44c19bb91ae507`. This package includes the final 1007-test source tree; it does not replace or rewrite the immutable D executor.
