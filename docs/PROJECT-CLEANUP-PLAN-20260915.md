# Project history and development-artifact cleanup — 2026-09-15

## Owner decision and outcome

The owner authorized removal of historical test, benchmark and strategy-research data, including the old research registry's experiments, definitions, precommits and recorded runs. Preserve historical market reports and position-ledger exports. Preserve the strategy-research engine and meaningful automated tests for future work. This is a current-tree cleanup, not a rewrite of Git commit history.

Implementation owner: a Luna xhigh subagent, working on the PR #13 branch in `/Users/eternal/.codex/worktrees/trading-pr13-performance`. Preserve unrelated local changes and verify the exact branch/head before deletion. The main checkout at `/Users/eternal/Desktop/Trading Claude Analytics` has separate ignored run output; remove only the explicitly inventoried disposable local output there, leaving its tracked `main` tree intact. Do not merge PR #13 as part of cleanup.

## Inventory already observed

The PR worktree is clean at `8507fac3cfa850d1def07181c3b88fbdb8d96e89`. It has about 183 GiB of ignored `.report-run/` data, concentrated in seven prior campaigns: recovered baseline (5.7 GiB), GC four-worker (11 GiB), GC comparison (23 GiB), heap expansion (31 GiB), calculation performance (56 GiB) and throughput search (56 GiB). Prior external archives under `/Users/eternal/.codex/artifacts/trading-pr13-performance/` total about 36 GiB. Tracked `strategy-research/v5-records/evidence/` is about 150 MiB and 979 files in the PR worktree. The main checkout has about 4.9 GiB of ignored `.report-run/` and a separate historical research evidence tree. All numbers are filesystem snapshots, not deletion targets by themselves.

Tracked repository inventory includes 1,198 `strategy-research/` files, 168 `reports/` files, 1,313 JSON documents and 192 JSON schemas. File extension alone is not a deletion criterion: schemas, runtime configuration, coverage source-map data, report-machine contracts and ledger exports remain needed.

## Deletion manifest and boundaries

Before editing, generate a machine-readable manifest of candidate tracked and ignored paths with path, bytes, tracked/ignored state, reason and dependency disposition. Derive paths from Git and filesystem inventory, not from wildcard deletion of `.json` or `.md`. Check for open Java, Maven, benchmark or archive processes and for mounts/symlinks before touching large local trees. Keep the manifest outside candidate deletion roots and retain it with the PR evidence so the result is reviewable.

Delete the historical research registry from the PR branch: `strategy-research/runs/`, `definitions/`, `experiments/`, `precommits/`, generated candidate and exposure records, old `v5-records/evidence/`, `receipts/`, `families/`, `data-backfill/`, `data-raw-replay/`, historical readiness records and generated registry indexes/performance catalogs. Remove retained historical executor packages and old development JSON receipts. The research engine remains capable of creating new registries and runs after cleanup. Keep `strategy-research/config/` only where referenced by current workflows/runtime contracts; keep reusable templates and the premise-first research protocol. Audit each top-level `strategy-research/` path before deleting it.

Delete dated development handoffs, plans, audits, runbooks and performance/evidence reports under `docs/` that exist only to describe removed campaigns. Replace the very large historical operating guides and `strategy-research/README.md` with concise current engine usage and status, removing broken historical commands/links. Retain essential ongoing Java and Codex setup docs, report publication instructions and source-map files when current tests or tools consume them. Remove test-result JSON and historical fixture data, but retain or replace small fixtures that verify real behavior.

Delete the named ignored `.report-run/`, `.research-run/` and `.calib-run/` campaign trees in the PR worktree and the main checkout, plus the trading-PR13 external artifact archives, **after** the tracked manifest and source checks pass. External archives are historical copies of these development/test campaigns and are within the owner's removal request. Remove stale build output under `target/` and other generated local caches only after relevant tests, so cleanup ends with a tidy workspace. Do not traverse out of the two checkout roots or the named artifact root; do not follow symlinks. Verify no active process uses the data. Record before/after `du` and filesystem checks. Ignored output has no Git recovery, so the final manifest must name its deletion explicitly.

Preserve `reports/` (including research retrospectives that live there) and `exports/` (historical position snapshots and committed signal feed), their source references, and the `.agents/skills/` strategy framework files/revision logs. Preserve `schemas/`, production Java, meaningful test suites, `.github/workflows/`, Maven wrapper/reactor, active report tools, `docs/java-source-map.json`, referenced attestation configuration and currently needed canonical input contracts. Do not delete live-market/accounting data, credentials, or unrelated files. Do not amend Hard Rules, research thresholds, strategy parameters, report schemas, or qualification claims as a side effect.

## Dependency repair

Search current source, tests, workflows, docs and skill files for every removed path family. About 33 current research tests refer directly to frozen FK-deleveraging experiment/evidence paths; those cannot simply be left broken. Migrate meaningful tests to small self-contained/generated fixtures or new test-resource inputs, preserving assertions about trust binding, economics, provenance, canonical publication and fail-closed behavior. Drop only tests whose sole purpose is asserting existence/hash of a retired historical archive; document why. Production source has hard-coded paths to the old experiment's portfolio policy/lifecycle timing; determine whether they are merely validation of caller-supplied historical commands or current engine defaults. Keep the generic engine runnable with fresh supplied inputs; replace obsolete built-in defaults only when behavior can be preserved and independently reviewed. The current CI workflow references `strategy-research/config/v5-attestation-key-registry.json`, which should remain.

The selected Mac engineering profile currently lives under historical evidence and contains links to deleted campaign packages. Its eight-seed benchmarking harness is itself historical development data, so remove the stale profile/bench instructions rather than leaving a non-runnable JSON as a current default. State the last measured local setting in a short historical note only if useful, clearly saying that the harness/package were removed. Clear stale `AGENTS.md` and runbook references to all deleted archives. Keep any future full research run's qualification policy explicit; removing old evidence does not make an unsupported machine qualified.

Rebuild generated research indexes only if the engine genuinely requires an empty valid registry. Otherwise remove old generated indexes and let future research create them. Run path/link checks and Git diff checks before validation. Never make a missing-file test pass by silently lowering a correctness assertion or changing a policy gate.

## Verification and publication

Independent review must check the proposed deletion manifest and dependency classification before execution, then review the final diff and changed tests. Run the pinned JDK21 clean reactor and affected CLI/research tests. Preserve the required changed-production-code 80% line/branch coverage gate if production Java is edited, measured against this reviewed starting head. Run current report lint/export-signals dry-run checks as needed to confirm market reports and feed remain usable; do not regenerate or rewrite their history. Check the chosen Git tree has no broken file references, all meaningful tests and CI pass, and `reports/`/`exports/` hashes and counts match the pre-cleanup snapshot. Independently spot-check the final deletion manifest against the actual diff and external-path cleanup. Record recovered disk space separately for tracked and ignored data.

Commit the scoped cleanup on the PR #13 branch, push, update PR description to describe the final behavior and deletion impact, and wait for checks on the exact pushed head. Leave the PR open and unmerged. Report any path whose removal would disable a still-needed engine, report or ledger function before deleting that path; otherwise finish the authorized cleanup autonomously.
