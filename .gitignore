# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Claude Code local overrides
.claude/settings.local.json

# Env / secrets (defensive — none expected, but never commit)
.env
.env.*
*.pem
*.key
credentials.json

# Cross-repo exchange (accounting → analytics).
# SECURITY: carries real balances, ACB cost basis, open positions, realized PnL.
# This repo auto-pushes EVERY commit to a public remote. Its real home is
# $TRADING_EXCHANGE_DIR, outside this tree — these entries are belt-and-braces.
position-snapshot.json
position-snapshot.*.json
/exchange/
/data/
*.snapshot.json
exports/*.tmp

# framework-calibration run scratch (corpus slices, digests, manifests) —
# reproducible from reports/ on demand, not repo state.
/.calib-run/
