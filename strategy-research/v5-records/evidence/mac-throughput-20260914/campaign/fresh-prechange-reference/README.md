# Fresh pre-change reference — 4 workers × 1536 MiB

This compact receipt bundle identifies the immutable pre-change JAR and manifest, the separately bound runtime profile/controller, and the PASS stage summary. The complete worker artifacts and raw logs are in the Phase 5 supplemental archive, which is recorded in the outer evidence manifest and archive custody receipt.

The prior 2026-09-13 comparison is retained as `historical-code-effect-comparison.json`; the primary fresh, descriptive comparison is `fresh-code-effect-comparison.json`. Both runs use the same eight task slots and settings. The new-code run occurred first, so this is not a randomized or statistically causal comparison.

No qualification, held-out research, or strategy change is implied.
